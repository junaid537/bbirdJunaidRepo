name: CWV PR Check

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  run-cwv-check:
    runs-on: ubuntu-latest
    environment: cwv-check-env-junaid

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install CWV Agent
        run: npm install --no-save git+https://github.com/ramboz/cwv-agent.git

      - name: Use hardcoded AEM URL
        id: extract_urls
        run: echo '["https://main--bbird--aemsites.aem.page/"]' > urls.json

      - name: Run CWV Agent for Each AEM URL
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
          AZURE_OPENAI_API_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_API_DEPLOYMENT_NAME }}
          AZURE_OPENAI_API_INSTANCE_NAME: ${{ secrets.AZURE_OPENAI_API_INSTANCE_NAME }}
          AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
          AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
          GOOGLE_PAGESPEED_INSIGHTS_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_INSIGHTS_API_KEY }}
          GOOGLE_CRUX_API_KEY: ${{ secrets.GOOGLE_CRUX_API_KEY }}
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
          PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome"
          MCP_SERVER_URL: ${{ secrets.MCP_SERVER_URL }}
        run: |
          set -x
          mkdir -p cwv-output
          echo "Running CWV audits for hardcoded AEM URL..."
          cat urls.json | jq -r '.[]' | while read URL; do
            echo "🔍 Auditing $URL"
            node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device mobile --model gpt-4o || echo "❌ Mobile audit failed"
            node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device desktop --model gpt-4o || echo "❌ Desktop audit failed"
          done
          echo "📂 Checking cache directory..."
          if [ -d ".cache" ]; then
            cp .cache/*.report.gpt4o.summary.md cwv-output/ 2>/dev/null || echo "⚠️ No summary reports found."
            cp .cache/*.suggestions.gpt4o.json cwv-output/ 2>/dev/null || echo "⚠️ No suggestions found."
            ls -la cwv-output/
          else
            echo "❌ Cache directory not found"
          fi

      - name: Upload Suggestions JSON
        uses: actions/upload-artifact@v4
        with:
          name: suggestions-json
          path: cwv-output/*.suggestions.gpt4o.json
          if-no-files-found: error
          retention-days: 1
      
      - name: Analyze Project Structure for Copilot Context
        id: analyze_structure
        run: |
          echo "📁 Analyzing project structure for Copilot context..."
          
          # Create a project context file for Copilot
          cat > project-context.md << 'EOF'
          # Project Structure Analysis for Core Web Vitals Optimization
          
          ## AEM Edge Delivery Services Project Structure
          EOF
          
          # Add common AEM EDS patterns
          if [ -d "scripts" ]; then
            echo "- ✅ /scripts/ directory found (JavaScript optimization target)" >> project-context.md
          fi
          if [ -d "styles" ]; then
            echo "- ✅ /styles/ directory found (CSS optimization target)" >> project-context.md  
          fi
          if [ -d "blocks" ]; then
            echo "- ✅ /blocks/ directory found (Block-specific optimizations)" >> project-context.md
          fi
          if [ -f "head.js" ]; then
            echo "- ✅ head.js found (Head script optimization target)" >> project-context.md
          fi
          
          echo "" >> project-context.md
          echo "## Key Files for Performance Optimization:" >> project-context.md
          find . -name "*.js" -o -name "*.css" | grep -E "(scripts|styles|blocks|head)" | head -20 >> project-context.md
          
          cat project-context.md

      - name: Verify Copilot Availability and Get Bot ID
        id: get_copilot_id
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.MY_PAT_TOKEN }}
          script: |
            // First, verify Copilot is available in this repository
            const query = `
              query($owner: String!, $name: String!) {
                repository(owner: $owner, name: $name) {
                  suggestedActors(capabilities: [CAN_BE_ASSIGNED], first: 100) {
                    nodes {
                      login
                      __typename
                      ... on Bot {
                        id
                      }
                      ... on User {
                        id
                      }
                    }
                  }
                }
              }
            `;
            
            const variables = {
              owner: context.repo.owner,
              name: context.repo.repo
            };
            
            try {
              const response = await github.graphql(query, variables);
              const suggestedActors = response.repository.suggestedActors.nodes;
              
              console.log('🔍 Available suggested actors:', JSON.stringify(suggestedActors, null, 2));
              
              // Find the Copilot coding agent
              const copilotAgent = suggestedActors.find(actor => 
                actor.login === 'copilot-swe-agent' || 
                actor.login === 'github-copilot[bot]'
              );
              
              if (copilotAgent) {
                console.log('✅ Copilot coding agent found:', copilotAgent);
                core.setOutput('copilot_available', 'true');
                core.setOutput('copilot_id', copilotAgent.id);
                core.setOutput('copilot_login', copilotAgent.login);
                return {
                  available: true,
                  id: copilotAgent.id,
                  login: copilotAgent.login
                };
              } else {
                console.log('❌ Copilot coding agent not found in suggested actors');
                console.log('Available actors:', suggestedActors.map(a => a.login));
                core.setOutput('copilot_available', 'false');
                return {
                  available: false,
                  message: 'Copilot coding agent not available in this repository'
                };
              }
            } catch (error) {
              console.log('❌ Error checking Copilot availability:', error);
              core.setOutput('copilot_available', 'false');
              return {
                available: false,
                error: error.message
              };
            }

      - name: Create GitHub Issues and Auto-Trigger Copilot PRs
        id: create_issues
        if: steps.get_copilot_id.outputs.copilot_available == 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.MY_PAT_TOKEN }}
          script: |
            const fs = require('fs');
            const path = require('path');
            
            const cwvOutputDir = 'cwv-output';
            const issueNumbers = [];
            const prNumber = context.issue.number;
            const copilotId = '${{ steps.get_copilot_id.outputs.copilot_id }}';
            const copilotLogin = '${{ steps.get_copilot_id.outputs.copilot_login }}';
            
            console.log('🤖 Using Copilot ID:', copilotId);
            console.log('🤖 Using Copilot Login:', copilotLogin);
            
            // Get the original PR author to assign final PRs to them
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });
            const originalAuthor = pr.data.user.login;
            

            // Helper functions
            function createIssueTitle(suggestion, url, deviceType) {
              const urlHost = new URL(url).hostname.split('.')[0];
              return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost}`;
            }
            
            function createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor) {
              return `## 🚀 Core Web Vitals Performance Issue
            **URL**: ${url}
            **Device**: ${deviceType} 
            **Metric**: ${suggestion.metric}
            **Priority**: ${suggestion.priority}
            **Effort**: ${suggestion.effort}
            **Expected Impact**: ${suggestion.impact}
            **Category**: ${suggestion.category}
            **Original PR Author**: @${originalAuthor}
            ## 📋 Description
            ${suggestion.description}
            ## 🛠️ Implementation Details
            ${suggestion.implementation || 'No specific implementation details provided.'}
            ${suggestion.codeExample ? `## 💻 Code Example\n\`\`\`\n${suggestion.codeExample}\n\`\`\`` : ''}
            ## 🎯 AEM-Specific Implementation Guide
            
            ### File Locations to Check:
            ${suggestion.category.toLowerCase() === 'javascript' ? `
            - \`/scripts/scripts.js\` - Main script entry point
            - \`/scripts/delayed.js\` - Non-critical scripts
            - \`/head.js\` - Head scripts
            - \`/blocks/*/\*.js\` - Block-specific scripts` : ''}
            ${suggestion.category.toLowerCase() === 'css' || suggestion.category.toLowerCase() === 'styles' ? `
            - \`/styles/styles.css\` - Main stylesheet
            - \`/blocks/*/\*.css\` - Block-specific styles
            - Check for critical CSS extraction opportunities` : ''}
            ${suggestion.category.toLowerCase() === 'images' ? `
            - Look for image references in blocks and templates
            - Implement proper \`loading\` and \`fetchpriority\` attributes
            - Check responsive image implementations` : ''}
            ### Performance Target:
            - **Metric**: ${suggestion.metric}
            - **Expected Improvement**: ${suggestion.impact}
            - **Device Focus**: ${deviceType}
            ---
            📝 *Auto-generated from PR #${prNumber}*
            🤖 *GitHub Copilot will automatically create a PR for this optimization*`;
            }
            
            // Function to sanitize category for use in labels
            function sanitizeCategory(category) {
              if (!category) return 'general';
              return category.toLowerCase()
                .replace(/[^a-z0-9-]/g, '-') // Replace non-alphanumeric with hyphens
                .replace(/-+/g, '-')         // Replace multiple hyphens with single
                .replace(/^-|-$/g, '');      // Remove leading/trailing hyphens
            }
            
            // Function to deduplicate suggestions within a file
            function deduplicateSuggestions(suggestions) {
              console.log(`🔍 Deduplicating ${suggestions.length} suggestions...`);
              
              const uniqueSuggestions = suggestions.filter((suggestion, index, self) => {
                // Create a unique key based on multiple criteria
                const suggestionKey = `${suggestion.title || ''}-${suggestion.metric || ''}-${suggestion.category || ''}-${(suggestion.description || '').substring(0, 100)}`;
                
                return index === self.findIndex(s => {
                  const otherKey = `${s.title || ''}-${s.metric || ''}-${s.category || ''}-${(s.description || '').substring(0, 100)}`;
                  return otherKey === suggestionKey;
                });
              });
              
              const duplicatesRemoved = suggestions.length - uniqueSuggestions.length;
              if (duplicatesRemoved > 0) {
                console.log(`✨ Removed ${duplicatesRemoved} duplicate suggestions`);
              }
              
              console.log(`📊 Final unique suggestions: ${uniqueSuggestions.length}`);
              return uniqueSuggestions;
            }
            
            async function isDuplicateIssue(title) {
              try {
                const issues = await github.rest.issues.listForRepo({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  state: 'open',
                  per_page: 100,
                  labels: 'cwv-suggestion'
                });
                
                return issues.data.some(issue => issue.title === title);
              } catch (error) {
                console.log('Error checking for duplicates:', error.message);
                return false;
              }
            }

            // FIXED: Proper Copilot assignment using GraphQL
            async function assignCopilotAgent(issueNodeId) {
              console.log(`🤖 Assigning Copilot agent to issue with node ID: ${issueNodeId}`);
              
              const mutation = `
                mutation($assignableId: ID!, $actorIds: [ID!]!) {
                  replaceActorsForAssignable(input: {
                    assignableId: $assignableId,
                    actorIds: $actorIds
                  }) {
                    assignable {
                      ... on Issue {
                        id
                        title
                        assignees(first: 10) {
                          nodes {
                            login
                          }
                        }
                      }
                    }
                  }
                }
              `;
              
              const variables = {
                assignableId: issueNodeId,
                actorIds: [copilotId]
              };
              
              try {
                const result = await github.graphql(mutation, variables);
                console.log('✅ Copilot agent successfully assigned to issue');
                console.log('📋 Current assignees:', result.replaceActorsForAssignable.assignable.assignees.nodes.map(n => n.login));
                return true;
              } catch (error) {
                console.log(`❌ Copilot assignment failed: ${error.message}`);
                console.log('🔍 Error details:', JSON.stringify(error, null, 2));
                return false;
              }
            }

            // Function to trigger Copilot PR creation automatically
            async function triggerCopilotPR(issueNumber, suggestion, url, deviceType, originalAuthor) {
              const copilotPrompt = `@${copilotLogin} please create a PR to fix this Core Web Vitals performance issue.

            **Implementation Requirements:**
            1. Create a new branch named: \`cwv-fix-${issueNumber}-${suggestion.metric.toLowerCase()}-${deviceType}\`
            2. Implement the optimization described in this issue
            3. Focus on improving ${suggestion.metric} metric by ${suggestion.impact}
            4. Target ${deviceType} device performance
            5. Create PR with detailed performance impact description
            6. Assign PR to @${originalAuthor}
            7. Link this issue to the PR

            **AEM-Specific Context:**
            - This is an AEM Edge Delivery Services project
            - Look for optimization opportunities in /scripts/, /styles/, /blocks/ directories
            - Maintain AEM EDS architectural patterns
            - Test URL: ${url}

            Please proceed with creating the optimization PR immediately.`;

              try {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  body: copilotPrompt
                });
                console.log(`✅ Triggered Copilot PR creation for issue #${issueNumber}`);
                return true;
              } catch (error) {
                console.log(`❌ Failed to trigger Copilot for issue #${issueNumber}: ${error.message}`);
                return false;
              }
            }
            
            try {
              if (!fs.existsSync(cwvOutputDir)) {
                console.log('❌ CWV output directory not found');
                return [];
              }
              
              const suggestionFiles = fs.readdirSync(cwvOutputDir)
                .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
              console.log(`Found ${suggestionFiles.length} suggestion files`);
              
              for (const filename of suggestionFiles) {
                const filepath = path.join(cwvOutputDir, filename);
                
                try {
                  const content = fs.readFileSync(filepath, 'utf8');
                  const data = JSON.parse(content);
                  
                  const url = data.url;
                  const deviceType = data.deviceType;
                  const suggestions = data.suggestions || [];
                  
                  console.log(`📄 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
                  // Deduplicate suggestions within this file
                  const uniqueSuggestions = deduplicateSuggestions(suggestions);
                  
                  for (const suggestion of uniqueSuggestions) {
                    const issueTitle = createIssueTitle(suggestion, url, deviceType);
                    
                    const isDuplicate = await isDuplicateIssue(issueTitle);
                    if (isDuplicate) {
                      console.log(`⚠️ Skipping duplicate issue: ${issueTitle}`);
                      continue;
                    }
                    
                    const issueBody = createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor);
                    
                    // Create labels array with proper sanitization
                    const labels = [
                      'cwv-suggestion',
                      'performance',
                      `cwv-${(suggestion.metric || 'unknown').toLowerCase().replace(/[^a-z0-9]/g, '-')}`,
                      `priority-${(suggestion.priority || 'medium').toLowerCase()}`,
                      `device-${deviceType}`,
                      `category-${sanitizeCategory(suggestion.category)}`,
                      'copilot-ready'
                    ];
                    
                    // Remove any empty or invalid labels
                    const validLabels = labels.filter(label => label && label.length > 0 && !label.includes(' '));
                    
                    try {
                      const issue = await github.rest.issues.create({
                        owner: context.repo.owner,
                        repo: context.repo.repo,
                        title: issueTitle,
                        body: issueBody,
                        labels: validLabels,
                      });
                      
                      issueNumbers.push(issue.data.number);
                      console.log(`✅ Created issue #${issue.data.number}: ${issueTitle}`);

                      // FIXED: Wait a moment, then assign Copilot
                      await new Promise(resolve => setTimeout(resolve, 2000));
                      
                      const assignmentSuccess = await assignCopilotAgent(issue.data.node_id);
                      
                      if (assignmentSuccess) {
                        // Wait a bit more before triggering the PR request
                        await new Promise(resolve => setTimeout(resolve, 3000));
                        
                        // Automatically trigger Copilot PR creation
                        const copilotTriggered = await triggerCopilotPR(
                          issue.data.number, 
                          suggestion, 
                          url, 
                          deviceType, 
                          originalAuthor
                        );
                        
                        if (copilotTriggered) {
                          console.log(`🤖 Copilot automatically triggered for issue #${issue.data.number}`);
                        }
                      } else {
                        console.log(`⚠️ Failed to assign Copilot to issue #${issue.data.number}, skipping PR trigger`);
                      }
                      
                      // Add a longer delay between issues to avoid rate limiting and give Copilot time to process
                      await new Promise(resolve => setTimeout(resolve, 5000));
                      
                    } catch (error) {
                      console.log(`❌ Failed to create issue for "${issueTitle}": ${error.message}`);
                      console.log(`Labels attempted: ${JSON.stringify(validLabels)}`);
                    }
                  }
                } catch (error) {
                  console.log(`❌ Failed to process file ${filename}: ${error.message}`);
                }
              }
              
              console.log(`\n📊 Summary: Created ${issueNumbers.length} issues with automatic Copilot PR triggers`);
              return issueNumbers;
              
            } catch (error) {
              console.log('❌ Error in issue creation process:', error.message);
              throw error;
            }


      - name: Handle Copilot Unavailable
        if: steps.get_copilot_id.outputs.copilot_available != 'true'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const comment = `## ❌ GitHub Copilot Coding Agent Not Available
            
            Unfortunately, the GitHub Copilot coding agent is not available in this repository. This could be because:
            
            - Copilot is not enabled for this repository
            - The coding agent feature is not activated
            - Repository permissions don't allow Copilot access
            
            **Manual Review Required**: CWV analysis completed but automatic PR creation is not possible.
            
            Please check your repository's Copilot settings or create the performance optimizations manually.`;
            
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: Comment on PR with Created Issues and Auto-PRs
        if: steps.create_issues.outputs.result != '[]'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumbers = ${{ steps.create_issues.outputs.result }};
            
            if (issueNumbers.length === 0) {
              console.log('No issues were created to comment about');
              return;
            }
            
            const issueLinks = issueNumbers.map(num => `- #${num}`).join('\n');
            
            const comment = `## 🚀 Core Web Vitals Analysis Complete - Fully Automated!
            ✨ **Automated Performance Optimization in Progress**
            I've analyzed your AEM pages and **automatically triggered ${issueNumbers.length} GitHub Copilot PRs** for performance optimizations:
            ${issueLinks}
            ### 🤖 What's Happening Automatically:
            - ✅ **Issues Created**: ${issueNumbers.length} specific performance optimizations identified  
            - 🔍 **Duplicates Filtered**: Removed duplicate suggestions from analysis files
            - 🤖 **Copilot Assigned**: GitHub Copilot coding agent properly assigned to each issue
            - 🚀 **PR Creation Triggered**: Copilot is now automatically creating PRs for each issue
            - 📊 **Metrics Analyzed**: LCP, CLS, and INP improvements calculated
            - 🎯 **Prioritized**: Issues ranked by impact and implementation effort
            - 📱 **Device-Specific**: Separate optimizations for mobile and desktop
            - 👤 **Auto-Assigned**: All generated PRs will be assigned to you (@${context.payload.pull_request.user.login})
            ### 📈 Expected Outcomes:
            - **Copilot Agent Properly Assigned** - Using correct GraphQL mutation
            - **Performance improvements** based on real Core Web Vitals data
            - **AEM-specific optimizations** targeting /scripts/, /styles/, /blocks/ directories
            - **Ready-to-merge PRs** with detailed implementation notes
            ### 🔄 Next Steps (5-10 minutes per issue):
            1. 🤖 Copilot analyzes your codebase for each optimization
            2. 🔧 PRs are created with specific performance fixes
            3. 📝 PRs include before/after performance impact descriptions
            4. ✅ PRs are automatically assigned to you for review
            5. 🔗 Issues are linked to their corresponding PRs
            **Monitor the "Pull Requests" tab** to see the automated PRs as they're created!
            ⏱️ *Each Copilot PR typically takes 5-10 minutes to be created*
            ---
            🎉 **Fully automated Core Web Vitals optimization powered by GitHub Copilot**`;
            
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: Upload Desktop Report
        uses: actions/upload-artifact@v4
        with:
          name: desktop-report
          path: cwv-output/*.desktop.report.gpt4o.summary.md
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Mobile Report
        uses: actions/upload-artifact@v4
        with:
          name: mobile-report
          path: cwv-output/*.mobile.report.gpt4o.summary.md
          if-no-files-found: warn
          retention-days: 7

      - name: Print project file tree (excluding node_modules)
        if: always()
        run: |
          sudo apt-get update && sudo apt-get install -y tree
          echo "📁 Project structure (excluding node_modules):"
          tree -L 3 -a -I 'node_modules'

# name: CWV PR Check

# on:
#   pull_request:
#     types: [opened, synchronize]

# permissions:
#   contents: write
#   pull-requests: write
#   issues: write

# jobs:
#   run-cwv-check:
#     runs-on: ubuntu-latest
#     environment: cwv-check-env-junaid

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install CWV Agent
#         run: npm install --no-save git+https://github.com/ramboz/cwv-agent.git

#       - name: Use hardcoded AEM URL
#         id: extract_urls
#         run: echo '["https://main--bbird--aemsites.aem.page/"]' > urls.json

#       - name: Run CWV Agent for Each AEM URL
#         env:
#           GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
#           AZURE_OPENAI_API_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_API_DEPLOYMENT_NAME }}
#           AZURE_OPENAI_API_INSTANCE_NAME: ${{ secrets.AZURE_OPENAI_API_INSTANCE_NAME }}
#           AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
#           AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
#           GOOGLE_PAGESPEED_INSIGHTS_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_INSIGHTS_API_KEY }}
#           GOOGLE_CRUX_API_KEY: ${{ secrets.GOOGLE_CRUX_API_KEY }}
#           PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
#           PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome"
#         run: |
#           set -x
#           mkdir -p cwv-output
#           echo "Running CWV audits for hardcoded AEM URL..."
#           cat urls.json | jq -r '.[]' | while read URL; do
#             echo "🔍 Auditing $URL"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device mobile --model gpt-4o || echo "❌ Mobile audit failed"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device desktop --model gpt-4o || echo "❌ Desktop audit failed"
#           done
#           echo "📂 Checking cache directory..."
#           if [ -d "node_modules/cwv-agent/.cache" ]; then
#             cp node_modules/cwv-agent/.cache/*.report.gpt4o.summary.md cwv-output/ 2>/dev/null || echo "⚠️ No summary reports found."
#             cp node_modules/cwv-agent/.cache/*.suggestions.gpt4o.json cwv-output/ 2>/dev/null || echo "⚠️ No suggestions found."
#             ls -la cwv-output/
#           else
#             echo "❌ Cache directory not found"
#           fi

#       - name: Upload Suggestions JSON
#         uses: actions/upload-artifact@v4
#         with:
#           name: suggestions-json
#           path: cwv-output/*.suggestions.gpt4o.json
#           if-no-files-found: error
#           retention-days: 1
      
#       - name: Analyze Project Structure for Copilot Context
#         id: analyze_structure
#         run: |
#           echo "📁 Analyzing project structure for Copilot context..."
          
#           # Create a project context file for Copilot
#           cat > project-context.md << 'EOF'
#           # Project Structure Analysis for Core Web Vitals Optimization
          
#           ## AEM Edge Delivery Services Project Structure
#           EOF
          
#           # Add common AEM EDS patterns
#           if [ -d "scripts" ]; then
#             echo "- ✅ /scripts/ directory found (JavaScript optimization target)" >> project-context.md
#           fi
#           if [ -d "styles" ]; then
#             echo "- ✅ /styles/ directory found (CSS optimization target)" >> project-context.md  
#           fi
#           if [ -d "blocks" ]; then
#             echo "- ✅ /blocks/ directory found (Block-specific optimizations)" >> project-context.md
#           fi
#           if [ -f "head.js" ]; then
#             echo "- ✅ head.js found (Head script optimization target)" >> project-context.md
#           fi
          
#           echo "" >> project-context.md
#           echo "## Key Files for Performance Optimization:" >> project-context.md
#           find . -name "*.js" -o -name "*.css" | grep -E "(scripts|styles|blocks|head)" | head -20 >> project-context.md
          
#           cat project-context.md
#       - name: Create GitHub Issues and Auto-Trigger Copilot PRs
#         id: create_issues
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.MY_PAT_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const path = require('path');
            
#             const cwvOutputDir = 'cwv-output';
#             const issueNumbers = [];
#             const prNumber = context.issue.number;
            
#             // Get the original PR author to assign final PRs to them
#             const pr = await github.rest.pulls.get({
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               pull_number: prNumber,
#             });
#             const originalAuthor = pr.data.user.login;
            
#             // Helper functions
#             function createIssueTitle(suggestion, url, deviceType) {
#               const urlHost = new URL(url).hostname.split('.')[0];
#               return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost}`;
#             }
            
#             function createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor) {
#               return `## 🚀 Core Web Vitals Performance Issue
#             **URL**: ${url}
#             **Device**: ${deviceType} 
#             **Metric**: ${suggestion.metric}
#             **Priority**: ${suggestion.priority}
#             **Effort**: ${suggestion.effort}
#             **Expected Impact**: ${suggestion.impact}
#             **Category**: ${suggestion.category}
#             **Original PR Author**: @${originalAuthor}
#             ## 📋 Description
#             ${suggestion.description}
#             ## 🛠️ Implementation Details
#             ${suggestion.implementation || 'No specific implementation details provided.'}
#             ${suggestion.codeExample ? `## 💻 Code Example\n\`\`\`\n${suggestion.codeExample}\n\`\`\`` : ''}
#             ## 🎯 AEM-Specific Implementation Guide
            
#             ### File Locations to Check:
#             ${suggestion.category.toLowerCase() === 'javascript' ? `
#             - \`/scripts/scripts.js\` - Main script entry point
#             - \`/scripts/delayed.js\` - Non-critical scripts
#             - \`/head.js\` - Head scripts
#             - \`/blocks/*/\*.js\` - Block-specific scripts` : ''}
#             ${suggestion.category.toLowerCase() === 'css' || suggestion.category.toLowerCase() === 'styles' ? `
#             - \`/styles/styles.css\` - Main stylesheet
#             - \`/blocks/*/\*.css\` - Block-specific styles
#             - Check for critical CSS extraction opportunities` : ''}
#             ${suggestion.category.toLowerCase() === 'images' ? `
#             - Look for image references in blocks and templates
#             - Implement proper \`loading\` and \`fetchpriority\` attributes
#             - Check responsive image implementations` : ''}
#             ### Performance Target:
#             - **Metric**: ${suggestion.metric}
#             - **Expected Improvement**: ${suggestion.impact}
#             - **Device Focus**: ${deviceType}
#             ---
#             📝 *Auto-generated from PR #${prNumber}*
#             🤖 *GitHub Copilot will automatically create a PR for this optimization*`;
#             }
            
#             // Function to sanitize category for use in labels
#             function sanitizeCategory(category) {
#               if (!category) return 'general';
#               return category.toLowerCase()
#                 .replace(/[^a-z0-9-]/g, '-') // Replace non-alphanumeric with hyphens
#                 .replace(/-+/g, '-')         // Replace multiple hyphens with single
#                 .replace(/^-|-$/g, '');      // Remove leading/trailing hyphens
#             }
            
#             // Function to deduplicate suggestions within a file
#             function deduplicateSuggestions(suggestions) {
#               console.log(`🔍 Deduplicating ${suggestions.length} suggestions...`);
              
#               const uniqueSuggestions = suggestions.filter((suggestion, index, self) => {
#                 // Create a unique key based on multiple criteria
#                 const suggestionKey = `${suggestion.title || ''}-${suggestion.metric || ''}-${suggestion.category || ''}-${(suggestion.description || '').substring(0, 100)}`;
                
#                 return index === self.findIndex(s => {
#                   const otherKey = `${s.title || ''}-${s.metric || ''}-${s.category || ''}-${(s.description || '').substring(0, 100)}`;
#                   return otherKey === suggestionKey;
#                 });
#               });
              
#               const duplicatesRemoved = suggestions.length - uniqueSuggestions.length;
#               if (duplicatesRemoved > 0) {
#                 console.log(`✨ Removed ${duplicatesRemoved} duplicate suggestions`);
#               }
              
#               console.log(`📊 Final unique suggestions: ${uniqueSuggestions.length}`);
#               return uniqueSuggestions;
#             }
            
#             async function isDuplicateIssue(title) {
#               try {
#                 const issues = await github.rest.issues.listForRepo({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   state: 'open',
#                   per_page: 100,
#                   labels: 'cwv-suggestion'
#                 });
                
#                 return issues.data.some(issue => issue.title === title);
#               } catch (error) {
#                 console.log('Error checking for duplicates:', error.message);
#                 return false;
#               }
#             }
#             async function assignCopilotAgent(issueNodeId) {
#               const token = core.getInput('github-token');
#               const copilotBotId = "BOT_kgDOC9w8XQ"; // This is Copilot Coding Agent's ID

#               const mutation = {
#                 query: `
#                   mutation($subjectId: ID!, $actorId: ID!) {
#                     assignSuggestedActor(input: {
#                       subjectId: $subjectId,
#                       actorId: $actorId
#                     }) {
#                       clientMutationId
#                     }
#                   }
#                 `,
#                 variables: {
#                   subjectId: issueNodeId,
#                   actorId: copilotBotId
#                 }
#               };

#               const response = await fetch('https://api.github.com/graphql', {
#                 method: 'POST',
#                 headers: {
#                   'Authorization': 'Bearer ' + token,
#                   'Content-Type': 'application/json'
#                 },
#                 body: JSON.stringify(mutation)
#               });

#               if (!response.ok) {
#                 console.log(`⚠️ Copilot assignment failed: ${response.statusText}`);
#               } else {
#                 console.log(`✅ Copilot agent assigned to issue.`);
#               }
#             }

#             // Function to trigger Copilot PR creation automatically
#             async function triggerCopilotPR(issueNumber, suggestion, url, deviceType, originalAuthor) {
#               const copilotPrompt = `@github-copilot please create a PR to fix this Core Web Vitals performance issue.
#             **Implementation Requirements:**
#             1. Create a new branch named: \`cwv-fix-${issueNumber}-${suggestion.metric.toLowerCase()}-${deviceType}\`
#             2. Implement the optimization described in this issue
#             3. Focus on improving ${suggestion.metric} metric by ${suggestion.impact}
#             4. Target ${deviceType} device performance
#             5. Create PR with detailed performance impact description
#             6. Assign PR to @${originalAuthor}
#             7. Link this issue to the PR
#             **AEM-Specific Context:**
#             - This is an AEM Edge Delivery Services project
#             - Look for optimization opportunities in /scripts/, /styles/, /blocks/ directories
#             - Maintain AEM EDS architectural patterns
#             - Test URL: ${url}
#             Please proceed with creating the optimization PR immediately.`;
#               try {
#                 await github.rest.issues.createComment({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   issue_number: issueNumber,
#                   body: copilotPrompt
#                 });
#                 console.log(`✅ Triggered Copilot PR creation for issue #${issueNumber}`);
#                 return true;
#               } catch (error) {
#                 console.log(`❌ Failed to trigger Copilot for issue #${issueNumber}: ${error.message}`);
#                 return false;
#               }
#             }
            
#             try {
#               if (!fs.existsSync(cwvOutputDir)) {
#                 console.log('❌ CWV output directory not found');
#                 return [];
#               }
              
#               const suggestionFiles = fs.readdirSync(cwvOutputDir)
#                 .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
#               console.log(`Found ${suggestionFiles.length} suggestion files`);
              
#               for (const filename of suggestionFiles) {
#                 const filepath = path.join(cwvOutputDir, filename);
                
#                 try {
#                   const content = fs.readFileSync(filepath, 'utf8');
#                   const data = JSON.parse(content);
                  
#                   const url = data.url;
#                   const deviceType = data.deviceType;
#                   const suggestions = data.suggestions || [];
                  
#                   console.log(`📄 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
#                   // Deduplicate suggestions within this file
#                   const uniqueSuggestions = deduplicateSuggestions(suggestions);
                  
#                   for (const suggestion of uniqueSuggestions) {
#                     const issueTitle = createIssueTitle(suggestion, url, deviceType);
                    
#                     const isDuplicate = await isDuplicateIssue(issueTitle);
#                     if (isDuplicate) {
#                       console.log(`⚠️ Skipping duplicate issue: ${issueTitle}`);
#                       continue;
#                     }
                    
#                     const issueBody = createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor);
                    
#                     // Create labels array with proper sanitization
#                     const labels = [
#                       'cwv-suggestion',
#                       'performance',
#                       `cwv-${(suggestion.metric || 'unknown').toLowerCase().replace(/[^a-z0-9]/g, '-')}`,
#                       `priority-${(suggestion.priority || 'medium').toLowerCase()}`,
#                       `device-${deviceType}`,
#                       `category-${sanitizeCategory(suggestion.category)}`,
#                       'copilot-ready'
#                     ];
                    
#                     // Remove any empty or invalid labels
#                     const validLabels = labels.filter(label => label && label.length > 0 && !label.includes(' '));
                    
#                     try {
#                       const issue = await github.rest.issues.create({
#                         owner: context.repo.owner,
#                         repo: context.repo.repo,
#                         title: issueTitle,
#                         body: issueBody,
#                         labels: validLabels,
#                       });
                      
#                       issueNumbers.push(issue.data.number);
#                       console.log(`✅ Created issue #${issue.data.number}: ${issueTitle}`);

#                       await assignCopilotAgent(issue.data.node_id);
#                       await triggerCopilotPR(
#                       issue.data.number, 
#                       suggestion, 
#                       url, 
#                       deviceType, 
#                       originalAuthor
#                     );

#                       // Wait a moment before triggering Copilot
#                       await new Promise(resolve => setTimeout(resolve, 1000));
                      
#                       // Automatically trigger Copilot PR creation
#                       const copilotTriggered = await triggerCopilotPR(
#                         issue.data.number, 
#                         suggestion, 
#                         url, 
#                         deviceType, 
#                         originalAuthor
#                       );
                      
#                       if (copilotTriggered) {
#                         console.log(`🤖 Copilot automatically triggered for issue #${issue.data.number}`);
#                       }
                      
#                       // Add a delay between issues to avoid rate limiting
#                       await new Promise(resolve => setTimeout(resolve, 2000));
                      
#                     } catch (error) {
#                       console.log(`❌ Failed to create issue for "${issueTitle}": ${error.message}`);
#                       console.log(`Labels attempted: ${JSON.stringify(validLabels)}`);
#                     }
#                   }
#                 } catch (error) {
#                   console.log(`❌ Failed to process file ${filename}: ${error.message}`);
#                 }
#               }
              
#               console.log(`\n📊 Summary: Created ${issueNumbers.length} issues with automatic Copilot PR triggers`);
#               return issueNumbers;
              
#             } catch (error) {
#               console.log('❌ Error in issue creation process:', error.message);
#               throw error;
#             }
#       - name: Comment on PR with Created Issues and Auto-PRs
#         if: steps.create_issues.outputs.result != '[]'
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const issueNumbers = ${{ steps.create_issues.outputs.result }};
            
#             if (issueNumbers.length === 0) {
#               console.log('No issues were created to comment about');
#               return;
#             }
            
#             const issueLinks = issueNumbers.map(num => `- #${num}`).join('\n');
            
#             const comment = `## 🚀 Core Web Vitals Analysis Complete - Fully Automated!
#             ✨ **Automated Performance Optimization in Progress**
#             I've analyzed your AEM pages and **automatically triggered ${issueNumbers.length} GitHub Copilot PRs** for performance optimizations:
#             ${issueLinks}
#             ### 🤖 What's Happening Automatically:
#             - ✅ **Issues Created**: ${issueNumbers.length} specific performance optimizations identified  
#             - 🔍 **Duplicates Filtered**: Removed duplicate suggestions from analysis files
#             - 🤖 **Copilot Triggered**: GitHub Copilot is now automatically creating PRs for each issue
#             - 📊 **Metrics Analyzed**: LCP, CLS, and INP improvements calculated
#             - 🎯 **Prioritized**: Issues ranked by impact and implementation effort
#             - 📱 **Device-Specific**: Separate optimizations for mobile and desktop
#             - 👤 **Auto-Assigned**: All generated PRs will be assigned to you (@${context.payload.pull_request.user.login})
#             ### 📈 Expected Outcomes:
#             - **No manual intervention required** - PRs are being created automatically
#             - **Performance improvements** based on real Core Web Vitals data
#             - **AEM-specific optimizations** targeting /scripts/, /styles/, /blocks/ directories
#             - **Ready-to-merge PRs** with detailed implementation notes
#             ### 🔄 Next Steps (All Automatic):
#             1. 🤖 Copilot analyzes your codebase for each optimization
#             2. 🔧 PRs are created with specific performance fixes
#             3. 📝 PRs include before/after performance impact descriptions
#             4. ✅ PRs are automatically assigned to you for review
#             5. 🔗 Issues are linked to their corresponding PRs
#             **Monitor the "Pull Requests" tab** to see the automated PRs as they're created!
#             ---
#             🎉 **Fully automated Core Web Vitals optimization powered by GitHub Copilot**
#             ⏱️ *PRs should appear within the next few minutes*`;
            
#             await github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: comment
#             });
#       - name: Upload Desktop Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: desktop-report
#           path: cwv-output/*.desktop.report.gpt4o.summary.md
#           if-no-files-found: warn
#           retention-days: 7

#       - name: Upload Mobile Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: mobile-report
#           path: cwv-output/*.mobile.report.gpt4o.summary.md
#           if-no-files-found: warn
#           retention-days: 7

#       - name: Upload Suggestions JSON
#         uses: actions/upload-artifact@v4
#         with:
#           name: suggestions-json
#           path: cwv-output/*.suggestions.gpt4o.json
#           if-no-files-found: warn
#           retention-days: 7

#       - name: Print project file tree (excluding node_modules)
#         if: always()
#         run: |
#           sudo apt-get update && sudo apt-get install -y tree
#           echo "📁 Project structure (excluding node_modules):"
#           tree -L 3 -a -I 'node_modules'

# GraphQL Logic

# name: CWV PR Check

# on:
#   pull_request:
#     types: [opened, synchronize]

# permissions:
#   contents: write
#   pull-requests: write
#   issues: write

# jobs:
#   run-cwv-check:
#     runs-on: ubuntu-latest
#     environment: cwv-check-env-junaid

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install CWV Agent
#         run: npm install --no-save git+https://github.com/ramboz/cwv-agent.git

#       - name: Use hardcoded AEM URL
#         id: extract_urls
#         run: echo '["https://main--bbird--aemsites.aem.page/"]' > urls.json

#       - name: Run CWV Agent for Each AEM URL
#         env:
#           GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
#           AZURE_OPENAI_API_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_API_DEPLOYMENT_NAME }}
#           AZURE_OPENAI_API_INSTANCE_NAME: ${{ secrets.AZURE_OPENAI_API_INSTANCE_NAME }}
#           AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
#           AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
#           GOOGLE_PAGESPEED_INSIGHTS_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_INSIGHTS_API_KEY }}
#           GOOGLE_CRUX_API_KEY: ${{ secrets.GOOGLE_CRUX_API_KEY }}
#           PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
#           PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome"
#         run: |
#           set -x
#           mkdir -p cwv-output
#           echo "Running CWV audits for hardcoded AEM URL..."
#           cat urls.json | jq -r '.[]' | while read URL; do
#             echo "🔍 Auditing $URL"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device mobile --model gpt-4o || echo "❌ Mobile audit failed"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device desktop --model gpt-4o || echo "❌ Desktop audit failed"
#           done
#           echo "📂 Checking cache directory..."
#           if [ -d "node_modules/cwv-agent/.cache" ]; then
#             cp node_modules/cwv-agent/.cache/*.report.gpt4o.summary.md cwv-output/ 2>/dev/null || echo "⚠️ No summary reports found."
#             cp node_modules/cwv-agent/.cache/*.suggestions.gpt4o.json cwv-output/ 2>/dev/null || echo "⚠️ No suggestions found."
#             ls -la cwv-output/
#           else
#             echo "❌ Cache directory not found"
#           fi

#       - name: Upload Desktop Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: desktop-report
#           path: cwv-output/*.desktop.report.gpt4o.summary.md
#           if-no-files-found: error
#           retention-days: 1

#       - name: Upload Mobile Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: mobile-report
#           path: cwv-output/*.mobile.report.gpt4o.summary.md
#           if-no-files-found: error
#           retention-days: 1

#       - name: Upload Suggestions JSON
#         uses: actions/upload-artifact@v4
#         with:
#           name: suggestions-json
#           path: cwv-output/*.suggestions.gpt4o.json
#           if-no-files-found: error
#           retention-days: 1

#       - name: Print project file tree (excluding node_modules)
#         run: |
#           sudo apt-get update && sudo apt-get install -y tree
#           echo "📁 Project structure (excluding node_modules):"
#           tree -L 3 -a -I 'node_modules'

#       - name: Create GitHub Issues and Assign Copilot
#         if: always()
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.MY_PAT_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const path = './cwv-output';
            
#             // Check if directory exists
#             if (!fs.existsSync(path)) {
#               console.log("⚠️ CWV output directory not found.");
#               return;
#             }
            
#             const file = fs.readdirSync(path).find(f => f.endsWith('.suggestions.gpt4o.json'));
#             if (!file) {
#               console.log("⚠️ No suggestions JSON found.");
#               return;
#             }

#             const suggestions = JSON.parse(fs.readFileSync(`${path}/${file}`, 'utf-8'));
#             console.log(`📄 Found ${suggestions.length} suggestions`);

#             // Use context instead of environment variables
#             const owner = context.repo.owner;
#             const repo = context.repo.repo;

#             const graphqlQuery = {
#               query: `
#                 query($owner: String!, $name: String!) {
#                   repository(owner: $owner, name: $name) {
#                     suggestedActors(capabilities: [CAN_BE_ASSIGNED], first: 100) {
#                       nodes { 
#                         login 
#                         id 
#                       }
#                     }
#                   }
#                 }
#               `,
#               variables: {
#                 owner: owner,
#                 name: repo
#               }
#             };

#             try {
#               const response = await fetch('https://api.github.com/graphql', {
#                 method: 'POST',
#                 headers: {
#                   'Authorization': `Bearer ${{ secrets.MY_PAT_TOKEN }}`,
#                   'Content-Type': 'application/json'
#                 },
#                 body: JSON.stringify(graphqlQuery)
#               });

#               if (!response.ok) {
#                 console.log(`❌ GraphQL request failed: ${response.status} ${response.statusText}`);
#                 return;
#               }

#               const data = await response.json();
#               console.log('GraphQL Response:', JSON.stringify(data, null, 2));
              
#               if (data.errors) {
#                 console.log('❌ GraphQL errors:', data.errors);
#                 return;
#               }

#               if (!data.data || !data.data.repository) {
#                 console.log('❌ No repository data found in response');
#                 return;
#               }

#               const copilot = data.data.repository.suggestedActors.nodes.find(n => n.login.includes('copilot'));
#               if (!copilot) {
#                 console.log("❌ Copilot not found in suggestedActors.");
#                 console.log("Available actors:", data.data.repository.suggestedActors.nodes.map(n => n.login));
#                 return;
#               }

#               for (const s of suggestions) {
#                 try {
#                   const issue = await github.rest.issues.create({
#                     owner,
#                     repo,
#                     title: `💡 CWV Suggestion: ${s.title || s.suggestion.slice(0, 70)}`,
#                     body: `**Suggestion**: ${s.suggestion}\n\n**Device**: ${s.device}\n**URL**: ${s.url}`,
#                     labels: ['copilot:run', 'copilot-ready']
#                   });

#                   const assignMutation = {
#                     query: `
#                       mutation($subjectId: ID!, $actorId: ID!) {
#                         assignSuggestedActor(input: {
#                           subjectId: $subjectId,
#                           actorId: $actorId
#                         }) {
#                           clientMutationId
#                         }
#                       }
#                     `,
#                     variables: {
#                       subjectId: issue.data.node_id,
#                       actorId: copilot.id
#                     }
#                   };

#                   const assignResponse = await fetch('https://api.github.com/graphql', {
#                     method: 'POST',
#                     headers: {
#                       'Authorization': `Bearer ${{ secrets.MY_PAT_TOKEN }}`,
#                       'Content-Type': 'application/json'
#                     },
#                     body: JSON.stringify(assignMutation)
#                   });

#                   if (!assignResponse.ok) {
#                     console.log(`⚠️ Failed to assign issue #${issue.data.number}: ${assignResponse.status}`);
#                   }

#                   console.log(`✅ Created & assigned issue #${issue.data.number}`);
#                 } catch (error) {
#                   console.log(`❌ Error creating issue for suggestion: ${error.message}`);
#                 }
#               }
#             } catch (error) {
#               console.log(`❌ Script error: ${error.message}`);
#             }




      

      # - name: Analyze Project Structure for Copilot Contexts
      #   id: analyze_structure
      #   run: |
      #     echo "📁 Analyzing project structure for Copilot context..."
          
      #     # Create a project context file for Copilot
      #     cat > project-context.md << 'EOF'
      #     # Project Structure Analysis for Core Web Vitals Optimization
          
      #     ## AEM Edge Delivery Services Project Structure
      #     EOF
          
      #     # Add common AEM EDS patterns
      #     if [ -d "scripts" ]; then
      #       echo "- ✅ /scripts/ directory found (JavaScript optimization target)" >> project-context.md
      #     fi
      #     if [ -d "styles" ]; then
      #       echo "- ✅ /styles/ directory found (CSS optimization target)" >> project-context.md  
      #     fi
      #     if [ -d "blocks" ]; then
      #       echo "- ✅ /blocks/ directory found (Block-specific optimizations)" >> project-context.md
      #     fi
      #     if [ -f "head.js" ]; then
      #       echo "- ✅ head.js found (Head script optimization target)" >> project-context.md
      #     fi
          
      #     echo "" >> project-context.md
      #     echo "## Key Files for Performance Optimization:" >> project-context.md
      #     find . -name "*.js" -o -name "*.css" | grep -E "(scripts|styles|blocks|head)" | head -20 >> project-context.md
          
      #     cat project-context.md

      # - name: Create Issues and Auto-Assign to Copilot
      #   id: create_issues_copilot
      #   uses: actions/github-script@v7
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       const fs = require('fs');
      #       const path = require('path');
            
      #       const cwvOutputDir = 'cwv-output';
      #       const createdIssues = [];
      #       const prNumber = context.issue.number;
            
      #       // Get the original PR details
      #       const pr = await github.rest.pulls.get({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         pull_number: prNumber,
      #       });
      #       const originalAuthor = pr.data.user.login;
            
      #       // Helper function to create issue title
      #       function createIssueTitle(suggestion, url, deviceType) {
      #         const urlHost = new URL(url).hostname.split('.')[0];
      #         const priority = suggestion.priority || 'medium';
      #         return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost} (${priority})`;
      #       }
            
      #       // Helper function to create detailed issue body for Copilot
      #       function createIssueBodyForCopilot(suggestion, url, deviceType) {
      #         return `## 🚀 Core Web Vitals Performance Optimization Task

      #       **URL**: ${url}
      #       **Device**: ${deviceType}
      #       **Metric**: ${suggestion.metric}
      #       **Priority**: ${suggestion.priority}
      #       **Expected Impact**: ${suggestion.impact}
      #       **Category**: ${suggestion.category}

      #       ## 📊 Performance Issue Description
      #       ${suggestion.description}

      #       ## 🎯 Optimization Requirements

      #       **Target Metrics:**
      #       - Primary: ${suggestion.metric}
      #       - Device: ${deviceType}
      #       - Priority Level: ${suggestion.priority}

      #       **Implementation Guidelines:**
      #       ${suggestion.implementation || 'Please implement based on the description above.'}

      #       ## 🛠️ Technical Context

      #       This is an AEM (Adobe Experience Manager) Edge Delivery Services project. The optimization should:

      #       1. **Follow AEM EDS best practices**
      #       2. **Target ${suggestion.metric} improvements**
      #       3. **Be ${deviceType}-optimized**
      #       4. **Maintain existing functionality**

      #       ## 📁 Relevant Files

      #       Based on the optimization category (${suggestion.category}), likely files to modify:
      #       - \`scripts/scripts.js\` - Main JavaScript functionality
      #       - \`styles/styles.css\` - Main stylesheet
      #       - \`scripts/lib-franklin.js\` - Franklin library extensions
      #       - Block-specific files if applicable

      #       ## 🧪 Acceptance Criteria

      #       - [ ] Implementation follows the performance optimization guidelines
      #       - [ ] Code is compatible with AEM Edge Delivery Services
      #       - [ ] Changes target the specified metric: **${suggestion.metric}**
      #       - [ ] Solution is optimized for **${deviceType}** devices
      #       - [ ] No breaking changes to existing functionality
      #       - [ ] Code includes appropriate comments explaining the optimization

      #       ## 📈 Success Metrics

      #       Expected improvement in **${suggestion.metric}** after implementation:
      #       ${suggestion.impact}

      #       ---

      #       **Original PR**: #${prNumber} by @${originalAuthor}
      #       **Effort Estimate**: ${suggestion.effort || 'TBD'}

      #       @github-copilot[bot] please implement this Core Web Vitals optimization following the requirements above.`;
      #       }
            
      #       try {
      #         if (!fs.existsSync(cwvOutputDir)) {
      #           console.log('❌ CWV output directory not found');
      #           return [];
      #         }
              
      #         const suggestionFiles = fs.readdirSync(cwvOutputDir)
      #           .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
      #         console.log(`📄 Found ${suggestionFiles.length} suggestion files to process`);
              
      #         for (const filename of suggestionFiles) {
      #           const filepath = path.join(cwvOutputDir, filename);
                
      #           try {
      #             const content = fs.readFileSync(filepath, 'utf8');
      #             const data = JSON.parse(content);
                  
      #             const url = data.url;
      #             const deviceType = data.deviceType;
      #             const suggestions = data.suggestions || [];
                  
      #             console.log(`🔍 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
      #             // Process top 3 suggestions per device/URL to avoid overwhelming Copilot
      #             for (const suggestion of suggestions.slice(0, 3)) {
      #               const issueTitle = createIssueTitle(suggestion, url, deviceType);
      #               const issueBody = createIssueBodyForCopilot(suggestion, url, deviceType);
                    
      #               // Create issue and immediately assign to GitHub Copilot
      #               const issue = await github.rest.issues.create({
      #                 owner: context.repo.owner,
      #                 repo: context.repo.repo,
      #                 title: issueTitle,
      #                 body: issueBody,
      #                 labels: [
      #                   'performance', 
      #                   'cwv-optimization', 
      #                   `device-${deviceType}`,
      #                   `metric-${suggestion.metric?.toLowerCase().replace(/\s+/g, '-')}`,
      #                   `priority-${suggestion.priority?.toLowerCase()}`,
      #                   'copilot-task'
      #                 ],
      #                 assignees: ['github-copilot[bot]'] // 🤖 AUTO-ASSIGN TO COPILOT!
      #               });
                    
      #               console.log(`✅ Created issue #${issue.data.number} and assigned to Copilot: ${issueTitle}`);
                    
      #               createdIssues.push({
      #                 issueNumber: issue.data.number,
      #                 title: issueTitle,
      #                 metric: suggestion.metric,
      #                 device: deviceType,
      #                 priority: suggestion.priority,
      #                 url: url,
      #                 assignedToCopilot: true
      #               });
                    
      #               // Rate limiting - be nice to GitHub API
      #               await new Promise(resolve => setTimeout(resolve, 2000));
      #             }
                  
      #           } catch (error) {
      #             console.log(`❌ Failed to process ${filename}: ${error.message}`);
      #           }
      #         }
              
      #         console.log(`\n🎉 Successfully created ${createdIssues.length} issues and assigned them to GitHub Copilot!`);
      #         return createdIssues;
              
      #       } catch (error) {
      #         console.log(`❌ Error in Copilot assignment process: ${error.message}`);
      #         throw error;
      #       }

      # - name: Comment on Original PR with Copilot Assignment Results  
      #   if: steps.create_issues_copilot.outputs.result != '[]'
      #   uses: actions/github-script@v7
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       const results = ${{ steps.create_issues_copilot.outputs.result }};
            
      #       if (results.length === 0) {
      #         console.log('No issues were created to comment about');
      #         return;
      #       }
            
      #       const itemsList = results.map(item => 
      #         `- **${item.title}**\n  - Issue: #${item.issueNumber} 🤖 *Assigned to GitHub Copilot*\n  - Metric: **${item.metric}** | Device: **${item.device}** | Priority: **${item.priority}**\n  - URL: ${item.url}`
      #       ).join('\n\n');
            
      #       const comment = `## 🤖 Automated CWV Optimization with GitHub Copilot!

      #       **✨ Successfully created ${results.length} performance optimization tasks and assigned them to GitHub Copilot:**

      #       ${itemsList}

      #       ### 🎯 What happens next:

      #       1. **🤖 Copilot Analysis**: GitHub Copilot will analyze each assigned issue
      #       2. **💻 Code Generation**: Copilot will create optimized code implementations  
      #       3. **📝 PR Creation**: Copilot will automatically create pull requests with fixes
      #       4. **👀 Review Ready**: You'll get notifications when PRs are ready for review

      #       ### 📊 Optimization Coverage:
      #       - **${results.filter(r => r.device === 'mobile').length} Mobile optimizations**
      #       - **${results.filter(r => r.device === 'desktop').length} Desktop optimizations**  
      #       - **Performance metrics targeted**: ${[...new Set(results.map(r => r.metric))].join(', ')}

      #       ### 🚀 Pro Tips:
      #       - Copilot will work on these issues in the background
      #       - Each PR will include detailed performance improvements
      #       - Review and test the PRs before merging
      #       - Monitor your Core Web Vitals scores after deployment

      #       ---
      #       🎉 **Fully automated CWV optimization powered by GitHub Copilot!**  
      #       *Copilot will handle the coding - you focus on reviewing and deploying.*`;
            
      #       await github.rest.issues.createComment({
      #         issue_number: context.issue.number,
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         body: comment
      #       });

      # - name: Upload Desktop Report
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: desktop-report
      #     path: cwv-output/*.desktop.report.gpt4o.summary.md
      #     if-no-files-found: error
      #     retention-days: 1

      # - name: Upload Mobile Report
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: mobile-report
      #     path: cwv-output/*.mobile.report.gpt4o.summary.md
      #     if-no-files-found: error
      #     retention-days: 1

      # - name: Upload Suggestions JSON
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: suggestions-json
      #     path: cwv-output/*.suggestions.gpt4o.json
      #     if-no-files-found: error
      #     retention-days: 1

      # - name: Print project file tree (excluding node_modules)
      #   run: |
      #     sudo apt-get update && sudo apt-get install -y tree
      #     echo "📁 Project structure (excluding node_modules):"
      #     tree -L 3 -a -I 'node_modules'

      # - name: Create GitHub Issues and Auto-Trigger Copilot PRs
      #   id: create_issues
      #   uses: actions/github-script@v7
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       const fs = require('fs');
      #       const path = require('path');
            
      #       const cwvOutputDir = 'cwv-output';
      #       const issueNumbers = [];
      #       const prNumber = context.issue.number;
            
      #       // Get the original PR author to assign final PRs to them
      #       const pr = await github.rest.pulls.get({
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         pull_number: prNumber,
      #       });
      #       const originalAuthor = pr.data.user.login;
            
      #       // Helper functions
      #       function createIssueTitle(suggestion, url, deviceType) {
      #         const urlHost = new URL(url).hostname.split('.')[0];
      #         return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost}`;
      #       }
            
      #       function createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor) {
      #         return `## 🚀 Core Web Vitals Performance Issue

      #       **URL**: ${url}
      #       **Device**: ${deviceType} 
      #       **Metric**: ${suggestion.metric}
      #       **Priority**: ${suggestion.priority}
      #       **Effort**: ${suggestion.effort}
      #       **Expected Impact**: ${suggestion.impact}
      #       **Category**: ${suggestion.category}
      #       **Original PR Author**: @${originalAuthor}

      #       ## 📋 Description
      #       ${suggestion.description}

      #       ## 🛠️ Implementation Details
      #       ${suggestion.implementation || 'No specific implementation details provided.'}

      #       ${suggestion.codeExample ? `## 💻 Code Example\n\`\`\`\n${suggestion.codeExample}\n\`\`\`` : ''}

      #       ## 🎯 AEM-Specific Implementation Guide
            
      #       ### File Locations to Check:
      #       ${suggestion.category.toLowerCase() === 'javascript' ? `
      #       - \`/scripts/scripts.js\` - Main script entry point
      #       - \`/scripts/delayed.js\` - Non-critical scripts
      #       - \`/head.js\` - Head scripts
      #       - \`/blocks/*/\*.js\` - Block-specific scripts` : ''}
      #       ${suggestion.category.toLowerCase() === 'css' || suggestion.category.toLowerCase() === 'styles' ? `
      #       - \`/styles/styles.css\` - Main stylesheet
      #       - \`/blocks/*/\*.css\` - Block-specific styles
      #       - Check for critical CSS extraction opportunities` : ''}
      #       ${suggestion.category.toLowerCase() === 'images' ? `
      #       - Look for image references in blocks and templates
      #       - Implement proper \`loading\` and \`fetchpriority\` attributes
      #       - Check responsive image implementations` : ''}

      #       ### Performance Target:
      #       - **Metric**: ${suggestion.metric}
      #       - **Expected Improvement**: ${suggestion.impact}
      #       - **Device Focus**: ${deviceType}

      #       ---
      #       📝 *Auto-generated from PR #${prNumber}*
      #       🤖 *GitHub Copilot will automatically create a PR for this optimization*`;
      #       }
            
      #       // Function to sanitize category for use in labels
      #       function sanitizeCategory(category) {
      #         if (!category) return 'general';
      #         return category.toLowerCase()
      #           .replace(/[^a-z0-9-]/g, '-') // Replace non-alphanumeric with hyphens
      #           .replace(/-+/g, '-')         // Replace multiple hyphens with single
      #           .replace(/^-|-$/g, '');      // Remove leading/trailing hyphens
      #       }
            
      #       // Function to deduplicate suggestions within a file
      #       function deduplicateSuggestions(suggestions) {
      #         console.log(`🔍 Deduplicating ${suggestions.length} suggestions...`);
              
      #         const uniqueSuggestions = suggestions.filter((suggestion, index, self) => {
      #           // Create a unique key based on multiple criteria
      #           const suggestionKey = `${suggestion.title || ''}-${suggestion.metric || ''}-${suggestion.category || ''}-${(suggestion.description || '').substring(0, 100)}`;
                
      #           return index === self.findIndex(s => {
      #             const otherKey = `${s.title || ''}-${s.metric || ''}-${s.category || ''}-${(s.description || '').substring(0, 100)}`;
      #             return otherKey === suggestionKey;
      #           });
      #         });
              
      #         const duplicatesRemoved = suggestions.length - uniqueSuggestions.length;
      #         if (duplicatesRemoved > 0) {
      #           console.log(`✨ Removed ${duplicatesRemoved} duplicate suggestions`);
      #         }
              
      #         console.log(`📊 Final unique suggestions: ${uniqueSuggestions.length}`);
      #         return uniqueSuggestions;
      #       }
            
      #       async function isDuplicateIssue(title) {
      #         try {
      #           const issues = await github.rest.issues.listForRepo({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             state: 'open',
      #             per_page: 100,
      #             labels: 'cwv-suggestion'
      #           });
                
      #           return issues.data.some(issue => issue.title === title);
      #         } catch (error) {
      #           console.log('Error checking for duplicates:', error.message);
      #           return false;
      #         }
      #       }

      #       // Function to trigger Copilot PR creation automatically
      #       async function triggerCopilotPR(issueNumber, suggestion, url, deviceType, originalAuthor) {
      #         const copilotPrompt = `Please create a PR to fix this Core Web Vitals performance issue.

      #       **Implementation Requirements:**
      #       1. Create a new branch named: \`cwv-fix-${issueNumber}-${suggestion.metric.toLowerCase()}-${deviceType}\`
      #       2. Implement the optimization described in this issue
      #       3. Focus on improving ${suggestion.metric} metric by ${suggestion.impact}
      #       4. Target ${deviceType} device performance
      #       5. Create PR with detailed performance impact description
      #       6. Copilot I want you to please create a PR for this issue
      #       7. Link this issue to the PR

      #       **AEM-Specific Context:**
      #       - This is an AEM Edge Delivery Services project
      #       - Look for optimization opportunities in /scripts/, /styles/, /blocks/ directories
      #       - Maintain AEM EDS architectural patterns
      #       - Test URL: ${url}

      #       Please proceed with creating the optimization PR immediately.`;

      #         try {
      #           await github.rest.issues.createComment({
      #             owner: context.repo.owner,
      #             repo: context.repo.repo,
      #             issue_number: issueNumber,
      #             body: copilotPrompt,
      #           });
      #           console.log(`✅ Triggered Copilot PR creation for issue #${issueNumber}`);
      #           return true;
      #         } catch (error) {
      #           console.log(`❌ Failed to trigger Copilot for issue #${issueNumber}: ${error.message}`);
      #           return false;
      #         }
      #       }
            
      #       try {
      #         if (!fs.existsSync(cwvOutputDir)) {
      #           console.log('❌ CWV output directory not found');
      #           return [];
      #         }
              
      #         const suggestionFiles = fs.readdirSync(cwvOutputDir)
      #           .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
      #         console.log(`Found ${suggestionFiles.length} suggestion files`);
              
      #         for (const filename of suggestionFiles) {
      #           const filepath = path.join(cwvOutputDir, filename);
                
      #           try {
      #             const content = fs.readFileSync(filepath, 'utf8');
      #             const data = JSON.parse(content);
                  
      #             const url = data.url;
      #             const deviceType = data.deviceType;
      #             const suggestions = data.suggestions || [];
                  
      #             console.log(`📄 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
      #             // Deduplicate suggestions within this file
      #             const uniqueSuggestions = deduplicateSuggestions(suggestions);
                  
      #             for (const suggestion of uniqueSuggestions) {
      #               const issueTitle = createIssueTitle(suggestion, url, deviceType);
                    
      #               const isDuplicate = await isDuplicateIssue(issueTitle);
      #               if (isDuplicate) {
      #                 console.log(`⚠️ Skipping duplicate issue: ${issueTitle}`);
      #                 continue;
      #               }
                    
      #               const issueBody = createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor);
                    
      #               // Create labels array with proper sanitization
      #               const labels = [
      #                 'copilot:run',
      #                 'performance',
      #                 `cwv-${(suggestion.metric || 'unknown').toLowerCase().replace(/[^a-z0-9]/g, '-')}`,
      #                 `priority-${(suggestion.priority || 'medium').toLowerCase()}`,
      #                 `device-${deviceType}`,
      #                 `category-${sanitizeCategory(suggestion.category)}`,
      #                 'copilot-ready'
      #               ];
                    
      #               // Remove any empty or invalid labels
      #               const validLabels = labels.filter(label => label && label.length > 0 && !label.includes(' '));
                    
      #               try {
      #                 const issue = await github.rest.issues.create({
      #                   owner: context.repo.owner,
      #                   repo: context.repo.repo,
      #                   title: issueTitle,
      #                   body: issueBody,
      #                   labels: validLabels,
      #                 });
                      
      #                 issueNumbers.push(issue.data.number);
      #                 console.log(`✅ Created issue #${issue.data.number}: ${issueTitle}`);
                      
      #                 // Wait a moment before triggering Copilot
      #                 await new Promise(resolve => setTimeout(resolve, 5000));
                      
      #                 // Automatically trigger Copilot PR creation
      #                 const copilotTriggered = await triggerCopilotPR(
      #                   issue.data.number, 
      #                   suggestion, 
      #                   url, 
      #                   deviceType, 
      #                   originalAuthor
      #                 );
      #                 if (copilotTriggered) {
      #                   console.log(`🤖 Copilot automatically triggered for issue #${issue.data.number}`);
      #                 }
                      
      #                 // Add a delay between issues to avoid rate limiting
      #                 await new Promise(resolve => setTimeout(resolve, 2000));
                      
      #               } catch (error) {
      #                 console.log(`❌ Failed to create issue for "${issueTitle}": ${error.message}`);
      #                 console.log(`Labels attempted: ${JSON.stringify(validLabels)}`);
      #               }
      #             }
      #           } catch (error) {
      #             console.log(`❌ Failed to process file ${filename}: ${error.message}`);
      #           }
      #         }
              
      #         console.log(`\n📊 Summary: Created ${issueNumbers.length} issues with automatic Copilot PR triggers`);
      #         return issueNumbers;
              
      #       } catch (error) {
      #         console.log('❌ Error in issue creation process:', error.message);
      #         throw error;
      #       }

      # - name: Comment on PR with Created Issues and Auto-PRs
      #   if: steps.create_issues.outputs.result != '[]'
      #   uses: actions/github-script@v7
      #   with:
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       const issueNumbers = ${{ steps.create_issues.outputs.result }};
            
      #       if (issueNumbers.length === 0) {
      #         console.log('No issues were created to comment about');
      #         return;
      #       }
            
      #       const issueLinks = issueNumbers.map(num => `- #${num}`).join('\n');
            
      #       const comment = `## 🚀 Core Web Vitals Analysis Complete - Fully Automated!

      #       ✨ **Automated Performance Optimization in Progress**

      #       I've analyzed your AEM pages and **automatically triggered ${issueNumbers.length} GitHub Copilot PRs** for performance optimizations:

      #       ${issueLinks}

      #       ### 🤖 What's Happening Automatically:
      #       - ✅ **Issues Created**: ${issueNumbers.length} specific performance optimizations identified  
      #       - 🔍 **Duplicates Filtered**: Removed duplicate suggestions from analysis files
      #       - 🤖 **Copilot Triggered**: GitHub Copilot is now automatically creating PRs for each issue
      #       - 📊 **Metrics Analyzed**: LCP, CLS, and INP improvements calculated
      #       - 🎯 **Prioritized**: Issues ranked by impact and implementation effort
      #       - 📱 **Device-Specific**: Separate optimizations for mobile and desktop
      #       - 👤 **Auto-Assigned**: All generated PRs will be assigned to you (@${context.payload.pull_request.user.login})

      #       ### 📈 Expected Outcomes:
      #       - **No manual intervention required** - PRs are being created automatically
      #       - **Performance improvements** based on real Core Web Vitals data
      #       - **AEM-specific optimizations** targeting /scripts/, /styles/, /blocks/ directories
      #       - **Ready-to-merge PRs** with detailed implementation notes

      #       ### 🔄 Next Steps (All Automatic):
      #       1. 🤖 Copilot analyzes your codebase for each optimization
      #       2. 🔧 PRs are created with specific performance fixes
      #       3. 📝 PRs include before/after performance impact descriptions
      #       4. ✅ PRs are automatically assigned to you for review
      #       5. 🔗 Issues are linked to their corresponding PRs

      #       **Monitor the "Pull Requests" tab** to see the automated PRs as they're created!

      #       ---
      #       🎉 **Fully automated Core Web Vitals optimization powered by GitHub Copilot**
      #       ⏱️ *PRs should appear within the next few minutes*`;
            
      #       await github.rest.issues.createComment({
      #         issue_number: context.issue.number,
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         body: comment
      #       });

      # - name: Upload Desktop Report
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: desktop-report
      #     path: cwv-output/*.desktop.report.gpt4o.summary.md
      #     if-no-files-found: warn
      #     retention-days: 7

      # - name: Upload Mobile Report
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: mobile-report
      #     path: cwv-output/*.mobile.report.gpt4o.summary.md
      #     if-no-files-found: warn
      #     retention-days: 7

      # - name: Upload Suggestions JSON
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: suggestions-json
      #     path: cwv-output/*.suggestions.gpt4o.json
      #     if-no-files-found: warn
      #     retention-days: 7

      # - name: Print project file tree (excluding node_modules)
      #   if: always()
      #   run: |
      #     sudo apt-get update && sudo apt-get install -y tree
      #     echo "📁 Project structure (excluding node_modules):"
      #     tree -L 3 -a -I 'node_modules'
# 222222name: CWV PR Check

# on:
#   pull_request:
#     types: [opened, synchronize]

# permissions:
#   contents: write
#   pull-requests: write
#   issues: write

# jobs:
#   run-cwv-check:
#     runs-on: ubuntu-latest
#     environment: cwv-check-env-junaid

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v3
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           fetch-depth: 0

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install CWV Agent
#         run: npm install --no-save git+https://github.com/ramboz/cwv-agent.git

#       - name: Use hardcoded AEM URL
#         id: extract_urls
#         run: echo '["https://main--bbird--aemsites.aem.page/"]' > urls.json

#       - name: Run CWV Agent for Each AEM URL
#         env:
#           GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
#           AZURE_OPENAI_API_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_API_DEPLOYMENT_NAME }}
#           AZURE_OPENAI_API_INSTANCE_NAME: ${{ secrets.AZURE_OPENAI_API_INSTANCE_NAME }}
#           AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
#           AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
#           GOOGLE_PAGESPEED_INSIGHTS_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_INSIGHTS_API_KEY }}
#           GOOGLE_CRUX_API_KEY: ${{ secrets.GOOGLE_CRUX_API_KEY }}
#           PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
#           PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome"
#         run: |
#           set -x
#           mkdir -p cwv-output
#           echo "Running CWV audits for listed AEM URLs..."
#           cat urls.json | jq -r '.[]' | while read URL; do
#             echo "🔍 Auditing $URL"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device mobile --model gpt-4o || echo "❌ Mobile audit failed"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device desktop --model gpt-4o || echo "❌ Desktop audit failed"
#           done
          
#           echo "📂 Checking cache directory..."
#           if [ -d "node_modules/cwv-agent/.cache" ]; then
#             cp node_modules/cwv-agent/.cache/*.report.gpt4o.summary.md cwv-output/ 2>/dev/null || echo "⚠️ No summary reports found."
#             cp node_modules/cwv-agent/.cache/*.suggestions.gpt4o.json cwv-output/ 2>/dev/null || echo "⚠️ No suggestions found."
#             ls -la cwv-output/
#           else
#             echo "❌ Cache directory not found"
#           fi

#       - name: Create Issues and Automated PRs
#         id: create_issues_and_prs
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const path = require('path');
            
#             const cwvOutputDir = 'cwv-output';
#             const createdItems = [];
#             const prNumber = context.issue.number;
            
#             // Get the original PR author
#             const pr = await github.rest.pulls.get({
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               pull_number: prNumber,
#             });
#             const originalAuthor = pr.data.user.login;
            
#             // Helper functions
#             function createIssueTitle(suggestion, url, deviceType) {
#               const urlHost = new URL(url).hostname.split('.')[0];
#               return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost}`;
#             }
            
#             function createBranchName(suggestion, deviceType, issueNumber) {
#               const metric = (suggestion.metric || 'performance').toLowerCase().replace(/[^a-z0-9]/g, '');
#               const category = (suggestion.category || 'optimization').toLowerCase().replace(/[^a-z0-9]/g, '');
#               return `cwv-fix-${issueNumber}-${metric}-${category}-${deviceType}`;
#             }
            
#             function generateOptimizationCode(suggestion, url, deviceType) {
#               // Generate actual code based on the suggestion
#               let codeChanges = [];
              
#               if (suggestion.category?.toLowerCase() === 'javascript') {
#                 codeChanges.push({
#                   file: 'scripts/scripts.js',
#                   content: `// CWV Optimization: ${suggestion.title}
#             // Metric: ${suggestion.metric} | Device: ${deviceType}
#             // Expected Impact: ${suggestion.impact}
            
#             // Defer non-critical JavaScript
#             function deferNonCritical() {
#               const scripts = document.querySelectorAll('script[data-defer="true"]');
#               scripts.forEach(script => {
#                 script.defer = true;
#               });
#             }
            
#             // Optimize script loading
#             function optimizeScriptLoading() {
#               // Implementation based on: ${suggestion.description}
#               ${suggestion.codeExample || '// Add specific optimization code here'}
#             }
            
#             // Initialize optimization
#             if (document.readyState === 'loading') {
#               document.addEventListener('DOMContentLoaded', optimizeScriptLoading);
#             } else {
#               optimizeScriptLoading();
#             }
#             `
#                 });
#               }
              
#               if (suggestion.category?.toLowerCase().includes('css') || suggestion.category?.toLowerCase().includes('style')) {
#                 codeChanges.push({
#                   file: 'styles/styles.css',
#                   content: `/* CWV Optimization: ${suggestion.title} */
#             /* Metric: ${suggestion.metric} | Device: ${deviceType} */
#             /* Expected Impact: ${suggestion.impact} */
            
#             /* Critical CSS optimization */
#             .optimize-loading {
#               content-visibility: auto;
#               contain-intrinsic-size: 200px;
#             }
            
#             /* Reduce layout shift */
#             img, video {
#               aspect-ratio: attr(width) / attr(height);
#               height: auto;
#             }
            
#             /* Implementation: ${suggestion.description} */
#             `
#                 });
#               }
              
#               if (suggestion.category?.toLowerCase().includes('image')) {
#                 codeChanges.push({
#                   file: 'scripts/lib-franklin.js',
#                   content: `// Image optimization for CWV
#             // ${suggestion.title}
            
#             function optimizeImages() {
#               const images = document.querySelectorAll('img');
#               images.forEach(img => {
#                 // Add loading="lazy" for non-critical images
#                 if (!img.closest('[data-critical]')) {
#                   img.loading = 'lazy';
#                 }
                
#                 // Add fetchpriority for LCP images
#                 if (img.closest('[data-lcp]')) {
#                   img.fetchPriority = 'high';
#                 }
#               });
#             }
            
#             // Initialize on DOM ready
#             document.addEventListener('DOMContentLoaded', optimizeImages);
#             `
#                 });
#               }
              
#               return codeChanges;
#             }
            
#             function createPRBody(suggestion, url, deviceType, issueNumber) {
#               return `## 🚀 CWV Performance Optimization PR

#             **Fixes Issue**: #${issueNumber}
#             **URL**: ${url}
#             **Device**: ${deviceType}
#             **Metric**: ${suggestion.metric}
#             **Expected Impact**: ${suggestion.impact}

#             ## 📊 Performance Optimization Details
            
#             - **Priority**: ${suggestion.priority}
#             - **Effort**: ${suggestion.effort}
#             - **Category**: ${suggestion.category}
            
#             ## 🛠️ Changes Made
            
#             ${suggestion.description}
            
#             ## 🎯 Implementation
            
#             This PR implements the following optimizations:
#             - Targeted ${suggestion.metric} improvements
#             - ${deviceType}-specific optimizations
#             - AEM EDS best practices maintained
            
#             ${suggestion.implementation ? `### Implementation Details:\n${suggestion.implementation}` : ''}
            
#             ## 🧪 Testing
            
#             Please test this optimization on:
#             - ${url}
#             - Both mobile and desktop devices
#             - Monitor ${suggestion.metric} metric improvements
            
#             ---
#             🤖 *Auto-generated PR for CWV optimization*
#             `;
#             }
            
#             async function createOptimizationPR(suggestion, url, deviceType, issueNumber) {
#               const branchName = createBranchName(suggestion, deviceType, issueNumber);
#               const prTitle = `CWV Fix: ${suggestion.title} (${deviceType})`;
#               const prBody = createPRBody(suggestion, url, deviceType, issueNumber);
              
#               try {
#                 // Get the default branch
#                 const repo = await github.rest.repos.get({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                 });
#                 const defaultBranch = repo.data.default_branch;
                
#                 // Get the latest commit SHA from default branch
#                 const ref = await github.rest.git.getRef({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   ref: `heads/${defaultBranch}`,
#                 });
#                 const baseSha = ref.data.object.sha;
                
#                 // Create new branch
#                 await github.rest.git.createRef({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   ref: `refs/heads/${branchName}`,
#                   sha: baseSha,
#                 });
                
#                 console.log(`✅ Created branch: ${branchName}`);
                
#                 // Generate and commit optimization code
#                 const codeChanges = generateOptimizationCode(suggestion, url, deviceType);
                
#                 for (const change of codeChanges) {
#                   try {
#                     // Try to get existing file
#                     let existingFile;
#                     let existingSha;
#                     try {
#                       const fileResponse = await github.rest.repos.getContent({
#                         owner: context.repo.owner,
#                         repo: context.repo.repo,
#                         path: change.file,
#                         ref: branchName,
#                       });
#                       existingFile = Buffer.from(fileResponse.data.content, 'base64').toString();
#                       existingSha = fileResponse.data.sha;
#                     } catch (error) {
#                       // File doesn't exist, we'll create it
#                       existingFile = '';
#                       existingSha = null;
#                     }
                    
#                     // Append or create file content
#                     const newContent = existingFile ? 
#                       `${existingFile}\n\n${change.content}` : 
#                       change.content;
                    
#                     // Create or update file
#                     await github.rest.repos.createOrUpdateFileContents({
#                       owner: context.repo.owner,
#                       repo: context.repo.repo,
#                       path: change.file,
#                       message: `CWV optimization: ${suggestion.title} (${deviceType})`,
#                       content: Buffer.from(newContent).toString('base64'),
#                       branch: branchName,
#                       sha: existingSha,
#                     });
                    
#                     console.log(`✅ Updated file: ${change.file}`);
#                   } catch (fileError) {
#                     console.log(`⚠️ Could not update ${change.file}: ${fileError.message}`);
#                   }
#                 }
                
#                 // Create pull request
#                 const pullRequest = await github.rest.pulls.create({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   title: prTitle,
#                   head: branchName,
#                   base: defaultBranch,
#                   body: prBody,
#                 });
                
#                 // Assign to original author
#                 await github.rest.issues.addAssignees({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   issue_number: pullRequest.data.number,
#                   assignees: [originalAuthor],
#                 });
                
#                 // Add labels
#                 await github.rest.issues.addLabels({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   issue_number: pullRequest.data.number,
#                   labels: ['performance', 'cwv-optimization', `device-${deviceType}`, `metric-${suggestion.metric?.toLowerCase()}`],
#                 });
                
#                 console.log(`✅ Created PR #${pullRequest.data.number}: ${prTitle}`);
#                 return pullRequest.data.number;
                
#               } catch (error) {
#                 console.log(`❌ Failed to create PR for ${suggestion.title}: ${error.message}`);
#                 return null;
#               }
#             }
            
#             // Main processing logic
#             try {
#               if (!fs.existsSync(cwvOutputDir)) {
#                 console.log('❌ CWV output directory not found');
#                 return [];
#               }
              
#               const suggestionFiles = fs.readdirSync(cwvOutputDir)
#                 .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
#               console.log(`Found ${suggestionFiles.length} suggestion files`);
              
#               for (const filename of suggestionFiles) {
#                 const filepath = path.join(cwvOutputDir, filename);
                
#                 try {
#                   const content = fs.readFileSync(filepath, 'utf8');
#                   const data = JSON.parse(content);
                  
#                   const url = data.url;
#                   const deviceType = data.deviceType;
#                   const suggestions = data.suggestions || [];
                  
#                   console.log(`📄 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
#                   for (const suggestion of suggestions.slice(0, 3)) { // Limit to 3 per device to avoid spam
#                     // Create issue
#                     const issueTitle = createIssueTitle(suggestion, url, deviceType);
#                     const issueBody = `## 🚀 Core Web Vitals Performance Issue

#             **URL**: ${url}
#             **Device**: ${deviceType}
#             **Metric**: ${suggestion.metric}
#             **Priority**: ${suggestion.priority}
#             **Expected Impact**: ${suggestion.impact}

#             ## Description
#             ${suggestion.description}

#             ## Implementation
#             ${suggestion.implementation || 'See automated PR for implementation details.'}

#             ---
#             🤖 *An automated PR will be created for this optimization*`;

#                     const issue = await github.rest.issues.create({
#                       owner: context.repo.owner,
#                       repo: context.repo.repo,
#                       title: issueTitle,
#                       body: issueBody,
#                       labels: ['performance', 'cwv-optimization', `device-${deviceType}`],
#                     });
                    
#                     console.log(`✅ Created issue #${issue.data.number}: ${issueTitle}`);
                    
#                     // Wait a moment then create PR
#                     await new Promise(resolve => setTimeout(resolve, 2000));
                    
#                     const prNumber = await createOptimizationPR(suggestion, url, deviceType, issue.data.number);
                    
#                     createdItems.push({
#                       issueNumber: issue.data.number,
#                       prNumber: prNumber,
#                       title: issueTitle,
#                       metric: suggestion.metric,
#                       device: deviceType
#                     });
                    
#                     // Rate limiting delay
#                     await new Promise(resolve => setTimeout(resolve, 3000));
#                   }
#                 } catch (error) {
#                   console.log(`❌ Failed to process file ${filename}: ${error.message}`);
#                 }
#               }
              
#               console.log(`\n📊 Summary: Created ${createdItems.length} issues with automated PRs`);
#               return createdItems;
              
#             } catch (error) {
#               console.log('❌ Error in automation process:', error.message);
#               throw error;
#             }

#       - name: Comment on Original PR with Results
#         if: steps.create_issues_and_prs.outputs.result != '[]'
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const results = ${{ steps.create_issues_and_prs.outputs.result }};
            
#             if (results.length === 0) {
#               console.log('No items were created to comment about');
#               return;
#             }
            
#             const itemsList = results.map(item => 
#               `- **${item.title}**\n  - Issue: #${item.issueNumber}\n  - PR: ${item.prNumber ? `#${item.prNumber}` : 'Failed to create'}\n  - Metric: ${item.metric} | Device: ${item.device}`
#             ).join('\n\n');
            
#             const comment = `## 🚀 Automated CWV Optimization Complete!

#             ✨ **Successfully created ${results.length} automated performance optimizations:**

#             ${itemsList}

#             ### 🎯 What was automated:
#             - ✅ **Issues Created**: Each with detailed performance analysis
#             - 🔧 **PRs Generated**: With actual code implementations  
#             - 📊 **Code Changes**: Targeted optimizations for each metric
#             - 🏷️ **Labels Applied**: Organized by device type and performance metric
#             - 👤 **Auto-Assigned**: All PRs assigned to @${context.payload.pull_request.user.login}

#             ### 📈 Next Steps:
#             1. **Review the PRs** - Each contains specific performance optimizations
#             2. **Test locally** - Verify the changes work with your AEM setup
#             3. **Measure impact** - Check Core Web Vitals improvements
#             4. **Merge when ready** - Performance gains should be measurable

#             ---
#             🎉 **Fully automated CWV optimization with real code changes!**`;
            
#             await github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: comment
#             });

#       - name: Upload Reports
#         uses: actions/upload-artifact@v4
#         with:
#           name: cwv-reports
#           path: cwv-output/
#           if-no-files-found: warn
#           retention-days: 7





# 2222name: CWV PR Check

# on:
#   pull_request:
#     types: [opened, synchronize]

# permissions:
#   contents: write
#   pull-requests: write
#   issues: write

# jobs:
#   run-cwv-check:
#     runs-on: ubuntu-latest
#     environment: cwv-check-env-junaid

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v3
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           fetch-depth: 0

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install CWV Agent
#         run: npm install --no-save git+https://github.com/ramboz/cwv-agent.git

#       - name: Use hardcoded AEM URL
#         id: extract_urls
#         run: echo '["https://main--bbird--aemsites.aem.page/"]' > urls.json

#       - name: Run CWV Agent for Each AEM URL
#         env:
#           GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
#           AZURE_OPENAI_API_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_API_DEPLOYMENT_NAME }}
#           AZURE_OPENAI_API_INSTANCE_NAME: ${{ secrets.AZURE_OPENAI_API_INSTANCE_NAME }}
#           AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
#           AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
#           GOOGLE_PAGESPEED_INSIGHTS_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_INSIGHTS_API_KEY }}
#           GOOGLE_CRUX_API_KEY: ${{ secrets.GOOGLE_CRUX_API_KEY }}
#           PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
#           PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome"
#         run: |
#           set -x
#           mkdir -p cwv-output
#           echo "Running CWV audits for listed AEM URLs..."
#           cat urls.json | jq -r '.[]' | while read URL; do
#             echo "🔍 Auditing $URL"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device mobile --model gpt-4o || echo "❌ Mobile audit failed"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device desktop --model gpt-4o || echo "❌ Desktop audit failed"
#           done
          
#           echo "📂 Checking cache directory..."
#           if [ -d "node_modules/cwv-agent/.cache" ]; then
#             cp node_modules/cwv-agent/.cache/*.report.gpt4o.summary.md cwv-output/ 2>/dev/null || echo "⚠️ No summary reports found."
#             cp node_modules/cwv-agent/.cache/*.suggestions.gpt4o.json cwv-output/ 2>/dev/null || echo "⚠️ No suggestions found."
#             ls -la cwv-output/
#           else
#             echo "❌ Cache directory not found"
#           fi

#       - name: Create Issues and Automated PRs
#         id: create_issues_and_prs
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const path = require('path');
            
#             const cwvOutputDir = 'cwv-output';
#             const createdItems = [];
#             const prNumber = context.issue.number;
            
#             // Get the original PR author
#             const pr = await github.rest.pulls.get({
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               pull_number: prNumber,
#             });
#             const originalAuthor = pr.data.user.login;
            
#             // Helper functions
#             function createIssueTitle(suggestion, url, deviceType) {
#               const urlHost = new URL(url).hostname.split('.')[0];
#               return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost}`;
#             }
            
#             function createBranchName(suggestion, deviceType, issueNumber) {
#               const metric = (suggestion.metric || 'performance').toLowerCase().replace(/[^a-z0-9]/g, '');
#               const category = (suggestion.category || 'optimization').toLowerCase().replace(/[^a-z0-9]/g, '');
#               return `cwv-fix-${issueNumber}-${metric}-${category}-${deviceType}`;
#             }
            
#             function generateOptimizationCode(suggestion, url, deviceType) {
#               // Generate actual code based on the suggestion
#               let codeChanges = [];
              
#               if (suggestion.category?.toLowerCase() === 'javascript') {
#                 codeChanges.push({
#                   file: 'scripts/scripts.js',
#                   content: `// CWV Optimization: ${suggestion.title}
#             // Metric: ${suggestion.metric} | Device: ${deviceType}
#             // Expected Impact: ${suggestion.impact}
            
#             // Defer non-critical JavaScript
#             function deferNonCritical() {
#               const scripts = document.querySelectorAll('script[data-defer="true"]');
#               scripts.forEach(script => {
#                 script.defer = true;
#               });
#             }
            
#             // Optimize script loading
#             function optimizeScriptLoading() {
#               // Implementation based on: ${suggestion.description}
#               ${suggestion.codeExample || '// Add specific optimization code here'}
#             }
            
#             // Initialize optimization
#             if (document.readyState === 'loading') {
#               document.addEventListener('DOMContentLoaded', optimizeScriptLoading);
#             } else {
#               optimizeScriptLoading();
#             }
#             `
#                 });
#               }
              
#               if (suggestion.category?.toLowerCase().includes('css') || suggestion.category?.toLowerCase().includes('style')) {
#                 codeChanges.push({
#                   file: 'styles/styles.css',
#                   content: `/* CWV Optimization: ${suggestion.title} */
#             /* Metric: ${suggestion.metric} | Device: ${deviceType} */
#             /* Expected Impact: ${suggestion.impact} */
            
#             /* Critical CSS optimization */
#             .optimize-loading {
#               content-visibility: auto;
#               contain-intrinsic-size: 200px;
#             }
            
#             /* Reduce layout shift */
#             img, video {
#               aspect-ratio: attr(width) / attr(height);
#               height: auto;
#             }
            
#             /* Implementation: ${suggestion.description} */
#             `
#                 });
#               }
              
#               if (suggestion.category?.toLowerCase().includes('image')) {
#                 codeChanges.push({
#                   file: 'scripts/lib-franklin.js',
#                   content: `// Image optimization for CWV
#             // ${suggestion.title}
            
#             function optimizeImages() {
#               const images = document.querySelectorAll('img');
#               images.forEach(img => {
#                 // Add loading="lazy" for non-critical images
#                 if (!img.closest('[data-critical]')) {
#                   img.loading = 'lazy';
#                 }
                
#                 // Add fetchpriority for LCP images
#                 if (img.closest('[data-lcp]')) {
#                   img.fetchPriority = 'high';
#                 }
#               });
#             }
            
#             // Initialize on DOM ready
#             document.addEventListener('DOMContentLoaded', optimizeImages);
#             `
#                 });
#               }
              
#               return codeChanges;
#             }
            
#             function createPRBody(suggestion, url, deviceType, issueNumber) {
#               return `## 🚀 CWV Performance Optimization PR

#             **Fixes Issue**: #${issueNumber}
#             **URL**: ${url}
#             **Device**: ${deviceType}
#             **Metric**: ${suggestion.metric}
#             **Expected Impact**: ${suggestion.impact}

#             ## 📊 Performance Optimization Details
            
#             - **Priority**: ${suggestion.priority}
#             - **Effort**: ${suggestion.effort}
#             - **Category**: ${suggestion.category}
            
#             ## 🛠️ Changes Made
            
#             ${suggestion.description}
            
#             ## 🎯 Implementation
            
#             This PR implements the following optimizations:
#             - Targeted ${suggestion.metric} improvements
#             - ${deviceType}-specific optimizations
#             - AEM EDS best practices maintained
            
#             ${suggestion.implementation ? `### Implementation Details:\n${suggestion.implementation}` : ''}
            
#             ## 🧪 Testing
            
#             Please test this optimization on:
#             - ${url}
#             - Both mobile and desktop devices
#             - Monitor ${suggestion.metric} metric improvements
            
#             ---
#             🤖 *Auto-generated PR for CWV optimization*
#             `;
#             }
            
#             async function createOptimizationPR(suggestion, url, deviceType, issueNumber) {
#               const branchName = createBranchName(suggestion, deviceType, issueNumber);
#               const prTitle = `CWV Fix: ${suggestion.title} (${deviceType})`;
#               const prBody = createPRBody(suggestion, url, deviceType, issueNumber);
              
#               try {
#                 // Get the default branch
#                 const repo = await github.rest.repos.get({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                 });
#                 const defaultBranch = repo.data.default_branch;
                
#                 // Get the latest commit SHA from default branch
#                 const ref = await github.rest.git.getRef({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   ref: `heads/${defaultBranch}`,
#                 });
#                 const baseSha = ref.data.object.sha;
                
#                 // Create new branch
#                 await github.rest.git.createRef({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   ref: `refs/heads/${branchName}`,
#                   sha: baseSha,
#                 });
                
#                 console.log(`✅ Created branch: ${branchName}`);
                
#                 // Generate and commit optimization code
#                 const codeChanges = generateOptimizationCode(suggestion, url, deviceType);
                
#                 for (const change of codeChanges) {
#                   try {
#                     // Try to get existing file
#                     let existingFile;
#                     let existingSha;
#                     try {
#                       const fileResponse = await github.rest.repos.getContent({
#                         owner: context.repo.owner,
#                         repo: context.repo.repo,
#                         path: change.file,
#                         ref: branchName,
#                       });
#                       existingFile = Buffer.from(fileResponse.data.content, 'base64').toString();
#                       existingSha = fileResponse.data.sha;
#                     } catch (error) {
#                       // File doesn't exist, we'll create it
#                       existingFile = '';
#                       existingSha = null;
#                     }
                    
#                     // Append or create file content
#                     const newContent = existingFile ? 
#                       `${existingFile}\n\n${change.content}` : 
#                       change.content;
                    
#                     // Create or update file
#                     await github.rest.repos.createOrUpdateFileContents({
#                       owner: context.repo.owner,
#                       repo: context.repo.repo,
#                       path: change.file,
#                       message: `CWV optimization: ${suggestion.title} (${deviceType})`,
#                       content: Buffer.from(newContent).toString('base64'),
#                       branch: branchName,
#                       sha: existingSha,
#                     });
                    
#                     console.log(`✅ Updated file: ${change.file}`);
#                   } catch (fileError) {
#                     console.log(`⚠️ Could not update ${change.file}: ${fileError.message}`);
#                   }
#                 }
                
#                 // Create pull request
#                 const pullRequest = await github.rest.pulls.create({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   title: prTitle,
#                   head: branchName,
#                   base: defaultBranch,
#                   body: prBody,
#                 });
                
#                 // Assign to original author
#                 await github.rest.issues.addAssignees({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   issue_number: pullRequest.data.number,
#                   assignees: [originalAuthor],
#                 });
                
#                 // Add labels
#                 await github.rest.issues.addLabels({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   issue_number: pullRequest.data.number,
#                   labels: ['performance', 'cwv-optimization', `device-${deviceType}`, `metric-${suggestion.metric?.toLowerCase()}`],
#                 });
                
#                 console.log(`✅ Created PR #${pullRequest.data.number}: ${prTitle}`);
#                 return pullRequest.data.number;
                
#               } catch (error) {
#                 console.log(`❌ Failed to create PR for ${suggestion.title}: ${error.message}`);
#                 return null;
#               }
#             }
            
#             // Main processing logic
#             try {
#               if (!fs.existsSync(cwvOutputDir)) {
#                 console.log('❌ CWV output directory not found');
#                 return [];
#               }
              
#               const suggestionFiles = fs.readdirSync(cwvOutputDir)
#                 .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
#               console.log(`Found ${suggestionFiles.length} suggestion files`);
              
#               for (const filename of suggestionFiles) {
#                 const filepath = path.join(cwvOutputDir, filename);
                
#                 try {
#                   const content = fs.readFileSync(filepath, 'utf8');
#                   const data = JSON.parse(content);
                  
#                   const url = data.url;
#                   const deviceType = data.deviceType;
#                   const suggestions = data.suggestions || [];
                  
#                   console.log(`📄 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
#                   for (const suggestion of suggestions.slice(0, 3)) { // Limit to 3 per device to avoid spam
#                     // Create issue
#                     const issueTitle = createIssueTitle(suggestion, url, deviceType);
#                     const issueBody = `## 🚀 Core Web Vitals Performance Issue

#             **URL**: ${url}
#             **Device**: ${deviceType}
#             **Metric**: ${suggestion.metric}
#             **Priority**: ${suggestion.priority}
#             **Expected Impact**: ${suggestion.impact}

#             ## Description
#             ${suggestion.description}

#             ## Implementation
#             ${suggestion.implementation || 'See automated PR for implementation details.'}

#             ---
#             🤖 *An automated PR will be created for this optimization*`;

#                     const issue = await github.rest.issues.create({
#                       owner: context.repo.owner,
#                       repo: context.repo.repo,
#                       title: issueTitle,
#                       body: issueBody,
#                       labels: ['performance', 'cwv-optimization', `device-${deviceType}`],
#                     });
                    
#                     console.log(`✅ Created issue #${issue.data.number}: ${issueTitle}`);
                    
#                     // Wait a moment then create PR
#                     await new Promise(resolve => setTimeout(resolve, 2000));
                    
#                     const prNumber = await createOptimizationPR(suggestion, url, deviceType, issue.data.number);
                    
#                     createdItems.push({
#                       issueNumber: issue.data.number,
#                       prNumber: prNumber,
#                       title: issueTitle,
#                       metric: suggestion.metric,
#                       device: deviceType
#                     });
                    
#                     // Rate limiting delay
#                     await new Promise(resolve => setTimeout(resolve, 3000));
#                   }
#                 } catch (error) {
#                   console.log(`❌ Failed to process file ${filename}: ${error.message}`);
#                 }
#               }
              
#               console.log(`\n📊 Summary: Created ${createdItems.length} issues with automated PRs`);
#               return createdItems;
              
#             } catch (error) {
#               console.log('❌ Error in automation process:', error.message);
#               throw error;
#             }

#       - name: Comment on Original PR with Results
#         if: steps.create_issues_and_prs.outputs.result != '[]'
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const results = ${{ steps.create_issues_and_prs.outputs.result }};
            
#             if (results.length === 0) {
#               console.log('No items were created to comment about');
#               return;
#             }
            
#             const itemsList = results.map(item => 
#               `- **${item.title}**\n  - Issue: #${item.issueNumber}\n  - PR: ${item.prNumber ? `#${item.prNumber}` : 'Failed to create'}\n  - Metric: ${item.metric} | Device: ${item.device}`
#             ).join('\n\n');
            
#             const comment = `## 🚀 Automated CWV Optimization Complete!

#             ✨ **Successfully created ${results.length} automated performance optimizations:**

#             ${itemsList}

#             ### 🎯 What was automated:
#             - ✅ **Issues Created**: Each with detailed performance analysis
#             - 🔧 **PRs Generated**: With actual code implementations  
#             - 📊 **Code Changes**: Targeted optimizations for each metric
#             - 🏷️ **Labels Applied**: Organized by device type and performance metric
#             - 👤 **Auto-Assigned**: All PRs assigned to @${context.payload.pull_request.user.login}

#             ### 📈 Next Steps:
#             1. **Review the PRs** - Each contains specific performance optimizations
#             2. **Test locally** - Verify the changes work with your AEM setup
#             3. **Measure impact** - Check Core Web Vitals improvements
#             4. **Merge when ready** - Performance gains should be measurable

#             ---
#             🎉 **Fully automated CWV optimization with real code changes!**`;
            
#             await github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: comment
#             });

#       - name: Upload Reports
#         uses: actions/upload-artifact@v4
#         with:
#           name: cwv-reports
#           path: cwv-output/
#           if-no-files-found: warn
#           retention-days: 7

# name: CWV PR Check

# on:
#   pull_request:
#     types: [opened, synchronize]

# permissions:
#   contents: write
#   pull-requests: write
#   issues: write


# jobs:
#   run-cwv-check:
#     runs-on: ubuntu-latest
#     environment: cwv-check-env-junaid

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Install CWV Agent
#         run: npm install --no-save git+https://github.com/ramboz/cwv-agent.git

#       - name: Use hardcoded AEM URL
#         id: extract_urls
#         run: echo '["https://main--bbird--aemsites.aem.page/"]' > urls.json

#       - name: Run CWV Agent for Each AEM URL
#         env:
#           GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}
#           AZURE_OPENAI_API_DEPLOYMENT_NAME: ${{ secrets.AZURE_OPENAI_API_DEPLOYMENT_NAME }}
#           AZURE_OPENAI_API_INSTANCE_NAME: ${{ secrets.AZURE_OPENAI_API_INSTANCE_NAME }}
#           AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
#           AZURE_OPENAI_API_VERSION: ${{ secrets.AZURE_OPENAI_API_VERSION }}
#           GOOGLE_PAGESPEED_INSIGHTS_API_KEY: ${{ secrets.GOOGLE_PAGESPEED_INSIGHTS_API_KEY }}
#           GOOGLE_CRUX_API_KEY: ${{ secrets.GOOGLE_CRUX_API_KEY }}
#           PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"
#           PUPPETEER_EXECUTABLE_PATH: "/usr/bin/google-chrome"
#         run: |
#           set -x  # enable debug logs
#           mkdir -p cwv-output
#           echo "Running CWV audits for listed AEM URLs..."
#           echo "Running CWV audits for hardcoded AEM URL..."
#           cat urls.json | jq -r '.[]' | while read URL; do
#             echo "🔍 Auditing $URL"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device mobile --model gpt-4o || echo "❌ Mobile audit failed"
#             node node_modules/cwv-agent/index.js --action prompt --url "$URL" --device desktop --model gpt-4o || echo "❌ Desktop audit failed"
#           done
          
#           echo "📂 Checking cache directory..."
#           if [ -d "node_modules/cwv-agent/.cache" ]; then
#             cp node_modules/cwv-agent/.cache/*.report.gpt4o.summary.md cwv-output/ 2>/dev/null || echo "⚠️ No summary reports found."
#             cp node_modules/cwv-agent/.cache/*.suggestions.gpt4o.json cwv-output/ 2>/dev/null || echo "⚠️ No suggestions found."
#             ls -la cwv-output/
#           else
#             echo "❌ Cache directory not found"
#           fi
#       - name: Analyze Project Structure for Copilot Context
#         id: analyze_structure
#         run: |
#           echo "📁 Analyzing project structure for Copilot context..."
          
#           # Create a project context file for Copilot
#           cat > project-context.md << 'EOF'
#           # Project Structure Analysis for Core Web Vitals Optimization
          
#           ## AEM Edge Delivery Services Project Structure
#           EOF
          
#           # Add common AEM EDS patterns
#           if [ -d "scripts" ]; then
#             echo "- ✅ /scripts/ directory found (JavaScript optimization target)" >> project-context.md
#           fi
#           if [ -d "styles" ]; then
#             echo "- ✅ /styles/ directory found (CSS optimization target)" >> project-context.md  
#           fi
#           if [ -d "blocks" ]; then
#             echo "- ✅ /blocks/ directory found (Block-specific optimizations)" >> project-context.md
#           fi
#           if [ -f "head.js" ]; then
#             echo "- ✅ head.js found (Head script optimization target)" >> project-context.md
#           fi
          
#           echo "" >> project-context.md
#           echo "## Key Files for Performance Optimization:" >> project-context.md
#           find . -name "*.js" -o -name "*.css" | grep -E "(scripts|styles|blocks|head)" | head -20 >> project-context.md
          
#           cat project-context.md


#       - name: Create GitHub Issues and Auto-Trigger Copilot PRs
#         id: create_issues
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const fs = require('fs');
#             const path = require('path');
            
#             const cwvOutputDir = 'cwv-output';
#             const issueNumbers = [];
#             const prNumber = context.issue.number;
            
#             // Get the original PR author to assign final PRs to them
#             const pr = await github.rest.pulls.get({
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               pull_number: prNumber,
#             });
#             const originalAuthor = pr.data.user.login;
            
#             // Helper functions
#             function createIssueTitle(suggestion, url, deviceType) {
#               const urlHost = new URL(url).hostname.split('.')[0];
#               return `[CWV-${deviceType.toUpperCase()}] ${suggestion.title} - ${urlHost}`;
#             }
            
#             function createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor) {
#               return `## 🚀 Core Web Vitals Performance Issue

#             **URL**: ${url}
#             **Device**: ${deviceType} 
#             **Metric**: ${suggestion.metric}
#             **Priority**: ${suggestion.priority}
#             **Effort**: ${suggestion.effort}
#             **Expected Impact**: ${suggestion.impact}
#             **Category**: ${suggestion.category}
#             **Original PR Author**: @${originalAuthor}

#             ## 📋 Description
#             ${suggestion.description}

#             ## 🛠️ Implementation Details
#             ${suggestion.implementation || 'No specific implementation details provided.'}

#             ${suggestion.codeExample ? `## 💻 Code Example\n\`\`\`\n${suggestion.codeExample}\n\`\`\`` : ''}

#             ## 🎯 AEM-Specific Implementation Guide
            
#             ### File Locations to Check:
#             ${suggestion.category.toLowerCase() === 'javascript' ? `
#             - \`/scripts/scripts.js\` - Main script entry point
#             - \`/scripts/delayed.js\` - Non-critical scripts
#             - \`/head.js\` - Head scripts
#             - \`/blocks/*/\*.js\` - Block-specific scripts` : ''}
#             ${suggestion.category.toLowerCase() === 'css' || suggestion.category.toLowerCase() === 'styles' ? `
#             - \`/styles/styles.css\` - Main stylesheet
#             - \`/blocks/*/\*.css\` - Block-specific styles
#             - Check for critical CSS extraction opportunities` : ''}
#             ${suggestion.category.toLowerCase() === 'images' ? `
#             - Look for image references in blocks and templates
#             - Implement proper \`loading\` and \`fetchpriority\` attributes
#             - Check responsive image implementations` : ''}

#             ### Performance Target:
#             - **Metric**: ${suggestion.metric}
#             - **Expected Improvement**: ${suggestion.impact}
#             - **Device Focus**: ${deviceType}

#             ---
#             📝 *Auto-generated from PR #${prNumber}*
#             🤖 *GitHub Copilot will automatically create a PR for this optimization*`;
#             }
            
#             // Function to sanitize category for use in labels
#             function sanitizeCategory(category) {
#               if (!category) return 'general';
#               return category.toLowerCase()
#                 .replace(/[^a-z0-9-]/g, '-') // Replace non-alphanumeric with hyphens
#                 .replace(/-+/g, '-')         // Replace multiple hyphens with single
#                 .replace(/^-|-$/g, '');      // Remove leading/trailing hyphens
#             }
            
#             // Function to deduplicate suggestions within a file
#             function deduplicateSuggestions(suggestions) {
#               console.log(`🔍 Deduplicating ${suggestions.length} suggestions...`);
              
#               const uniqueSuggestions = suggestions.filter((suggestion, index, self) => {
#                 // Create a unique key based on multiple criteria
#                 const suggestionKey = `${suggestion.title || ''}-${suggestion.metric || ''}-${suggestion.category || ''}-${(suggestion.description || '').substring(0, 100)}`;
                
#                 return index === self.findIndex(s => {
#                   const otherKey = `${s.title || ''}-${s.metric || ''}-${s.category || ''}-${(s.description || '').substring(0, 100)}`;
#                   return otherKey === suggestionKey;
#                 });
#               });
              
#               const duplicatesRemoved = suggestions.length - uniqueSuggestions.length;
#               if (duplicatesRemoved > 0) {
#                 console.log(`✨ Removed ${duplicatesRemoved} duplicate suggestions`);
#               }
              
#               console.log(`📊 Final unique suggestions: ${uniqueSuggestions.length}`);
#               return uniqueSuggestions;
#             }
            
#             async function isDuplicateIssue(title) {
#               try {
#                 const issues = await github.rest.issues.listForRepo({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   state: 'open',
#                   per_page: 100,
#                   labels: 'cwv-suggestion'
#                 });
                
#                 return issues.data.some(issue => issue.title === title);
#               } catch (error) {
#                 console.log('Error checking for duplicates:', error.message);
#                 return false;
#               }
#             }

#             // Function to trigger Copilot PR creation automatically
#             async function triggerCopilotPR(issueNumber, suggestion, url, deviceType, originalAuthor) {
#               const copilotPrompt = `Please create a PR to fix this Core Web Vitals performance issue.

#             **Implementation Requirements:**
#             1. Create a new branch named: \`cwv-fix-${issueNumber}-${suggestion.metric.toLowerCase()}-${deviceType}\`
#             2. Implement the optimization described in this issue
#             3. Focus on improving ${suggestion.metric} metric by ${suggestion.impact}
#             4. Target ${deviceType} device performance
#             5. Create PR with detailed performance impact description
#             6. Assign PR to @${github-copilot}
#             7. Link this issue to the PR

#             **AEM-Specific Context:**
#             - This is an AEM Edge Delivery Services project
#             - Look for optimization opportunities in /scripts/, /styles/, /blocks/ directories
#             - Maintain AEM EDS architectural patterns
#             - Test URL: ${url}

#             Please proceed with creating the optimization PR immediately.`;

#               try {
#                 await github.rest.issues.createComment({
#                   owner: context.repo.owner,
#                   repo: context.repo.repo,
#                   issue_number: issueNumber,
#                   body: copilotPrompt,
#                 });
#                 console.log(`✅ Triggered Copilot PR creation for issue #${issueNumber}`);
#                 return true;
#               } catch (error) {
#                 console.log(`❌ Failed to trigger Copilot for issue #${issueNumber}: ${error.message}`);
#                 return false;
#               }
#             }
            
#             try {
#               if (!fs.existsSync(cwvOutputDir)) {
#                 console.log('❌ CWV output directory not found');
#                 return [];
#               }
              
#               const suggestionFiles = fs.readdirSync(cwvOutputDir)
#                 .filter(file => file.endsWith('.suggestions.gpt4o.json'));
              
#               console.log(`Found ${suggestionFiles.length} suggestion files`);
              
#               for (const filename of suggestionFiles) {
#                 const filepath = path.join(cwvOutputDir, filename);
                
#                 try {
#                   const content = fs.readFileSync(filepath, 'utf8');
#                   const data = JSON.parse(content);
                  
#                   const url = data.url;
#                   const deviceType = data.deviceType;
#                   const suggestions = data.suggestions || [];
                  
#                   console.log(`📄 Processing ${suggestions.length} suggestions for ${url} (${deviceType})`);
                  
#                   // Deduplicate suggestions within this file
#                   const uniqueSuggestions = deduplicateSuggestions(suggestions);
                  
#                   for (const suggestion of uniqueSuggestions) {
#                     const issueTitle = createIssueTitle(suggestion, url, deviceType);
                    
#                     const isDuplicate = await isDuplicateIssue(issueTitle);
#                     if (isDuplicate) {
#                       console.log(`⚠️ Skipping duplicate issue: ${issueTitle}`);
#                       continue;
#                     }
                    
#                     const issueBody = createIssueBody(suggestion, url, deviceType, prNumber, originalAuthor);
                    
#                     // Create labels array with proper sanitization
#                     const labels = [
#                       'copilot:run',
#                       'performance',
#                       `cwv-${(suggestion.metric || 'unknown').toLowerCase().replace(/[^a-z0-9]/g, '-')}`,
#                       `priority-${(suggestion.priority || 'medium').toLowerCase()}`,
#                       `device-${deviceType}`,
#                       `category-${sanitizeCategory(suggestion.category)}`,
#                       'copilot-ready'
#                     ];
                    
#                     // Remove any empty or invalid labels
#                     const validLabels = labels.filter(label => label && label.length > 0 && !label.includes(' '));
                    
#                     try {
#                       const issue = await github.rest.issues.create({
#                         owner: context.repo.owner,
#                         repo: context.repo.repo,
#                         title: issueTitle,
#                         body: issueBody,
#                         labels: validLabels,
#                       });
                      
#                       issueNumbers.push(issue.data.number);
#                       console.log(`✅ Created issue #${issue.data.number}: ${issueTitle}`);
                      
#                       // Wait a moment before triggering Copilot
#                       await new Promise(resolve => setTimeout(resolve, 5000));
                      
#                       // Automatically trigger Copilot PR creation
#                       const copilotTriggered = await triggerCopilotPR(
#                         issue.data.number, 
#                         suggestion, 
#                         url, 
#                         deviceType, 
#                         originalAuthor
#                       );
#                       if (copilotTriggered) {
#                         console.log(`🤖 Copilot automatically triggered for issue #${issue.data.number}`);
#                       }
                      
#                       // Add a delay between issues to avoid rate limiting
#                       await new Promise(resolve => setTimeout(resolve, 2000));
                      
#                     } catch (error) {
#                       console.log(`❌ Failed to create issue for "${issueTitle}": ${error.message}`);
#                       console.log(`Labels attempted: ${JSON.stringify(validLabels)}`);
#                     }
#                   }
#                 } catch (error) {
#                   console.log(`❌ Failed to process file ${filename}: ${error.message}`);
#                 }
#               }
              
#               console.log(`\n📊 Summary: Created ${issueNumbers.length} issues with automatic Copilot PR triggers`);
#               return issueNumbers;
              
#             } catch (error) {
#               console.log('❌ Error in issue creation process:', error.message);
#               throw error;
#             }

#       - name: Comment on PR with Created Issues and Auto-PRs
#         if: steps.create_issues.outputs.result != '[]'
#         uses: actions/github-script@v7
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           script: |
#             const issueNumbers = ${{ steps.create_issues.outputs.result }};
            
#             if (issueNumbers.length === 0) {
#               console.log('No issues were created to comment about');
#               return;
#             }
            
#             const issueLinks = issueNumbers.map(num => `- #${num}`).join('\n');
            
#             const comment = `## 🚀 Core Web Vitals Analysis Complete - Fully Automated!

#             ✨ **Automated Performance Optimization in Progress**

#             I've analyzed your AEM pages and **automatically triggered ${issueNumbers.length} GitHub Copilot PRs** for performance optimizations:

#             ${issueLinks}

#             ### 🤖 What's Happening Automatically:
#             - ✅ **Issues Created**: ${issueNumbers.length} specific performance optimizations identified  
#             - 🔍 **Duplicates Filtered**: Removed duplicate suggestions from analysis files
#             - 🤖 **Copilot Triggered**: GitHub Copilot is now automatically creating PRs for each issue
#             - 📊 **Metrics Analyzed**: LCP, CLS, and INP improvements calculated
#             - 🎯 **Prioritized**: Issues ranked by impact and implementation effort
#             - 📱 **Device-Specific**: Separate optimizations for mobile and desktop
#             - 👤 **Auto-Assigned**: All generated PRs will be assigned to you (@${context.payload.pull_request.user.login})

#             ### 📈 Expected Outcomes:
#             - **No manual intervention required** - PRs are being created automatically
#             - **Performance improvements** based on real Core Web Vitals data
#             - **AEM-specific optimizations** targeting /scripts/, /styles/, /blocks/ directories
#             - **Ready-to-merge PRs** with detailed implementation notes

#             ### 🔄 Next Steps (All Automatic):
#             1. 🤖 Copilot analyzes your codebase for each optimization
#             2. 🔧 PRs are created with specific performance fixes
#             3. 📝 PRs include before/after performance impact descriptions
#             4. ✅ PRs are automatically assigned to you for review
#             5. 🔗 Issues are linked to their corresponding PRs

#             **Monitor the "Pull Requests" tab** to see the automated PRs as they're created!

#             ---
#             🎉 **Fully automated Core Web Vitals optimization powered by GitHub Copilot**
#             ⏱️ *PRs should appear within the next few minutes*`;
            
#             await github.rest.issues.createComment({
#               issue_number: context.issue.number,
#               owner: context.repo.owner,
#               repo: context.repo.repo,
#               body: comment
#             });

#       - name: Upload Desktop Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: desktop-report
#           path: cwv-output/*.desktop.report.gpt4o.summary.md
#           if-no-files-found: warn
#           retention-days: 7

#       - name: Upload Mobile Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: mobile-report
#           path: cwv-output/*.mobile.report.gpt4o.summary.md
#           if-no-files-found: warn
#           retention-days: 7

#       - name: Upload Suggestions JSON
#         uses: actions/upload-artifact@v4
#         with:
#           name: suggestions-json
#           path: cwv-output/*.suggestions.gpt4o.json
#           if-no-files-found: warn
#           retention-days: 7

#       - name: Print project file tree (excluding node_modules)
#         if: always()
#         run: |
#           sudo apt-get update && sudo apt-get install -y tree
#           echo "📁 Project structure (excluding node_modules):"
#           tree -L 3 -a -I 'node_modules'